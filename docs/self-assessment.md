# Self-assessment against the trsdn Repository Quality Standard

- Assessed on: 2026-09-22
- Standard version: [1.20.0](https://github.com/trsdn/.github/blob/v1.20.0/docs/repository-quality-standard.md)
- Result: 70 pass, 13 partial, 3 fail, 21 not applicable, 0 unknown
- State: **Needs work**. There are recorded `Fail` results and none of them is on a critical criterion, which is the standard's rule for this state.

Re-assessed in full against standard 1.20.0 (previous record: 1.15.0, 2026-09-21). Every criterion was assessed by an AI agent that read the standard's text for it and the repository, and each `Fail` and `Partial` was then re-checked by a second agent that tried to refute it; two results changed under that check (`I04` to pass, `X03` to fail). `I02` and `I03` remain fail: the fix is in the source (commit 58aea83) but postdates the v0.5.0 tag, so it is not in the published release the standard reads. Nothing was left for the maintainer to decide.

## Gaps

| Criterion | Result | Evidence |
|---|---|---|
| `B06` | partial | Confirmed the colleague's 'partial'. Alerts part met: gh api dependabot/alerts?state=open&severity=critical, code-scanning/alerts?state=open&severity=critical, and secret-scanning/alerts?state=open each return [] — no open critical/secret alerts. Merge-policy part not met: gh api repos/trsdn/OpenConnct shows allow_squash_merge, allow_merge_commit, allow_rebase_merge all true; the sole ruleset (protect-main, id 23763420, created 2026-09-21) carries only deletion, non_fast_forward, and required_status_checks — no pull_request rule requiring PRs before merge; classic branch protection returns 404 (none); and grep of README.md, CONTRIBUTING.md, AGENTS.md, and docs/ finds no stated merge method. The standard's single-maintainer exception (docs/self-assessment.md previously invoked it under 1.15.0, when the ruleset returned 404) no longer applies now that a ruleset exists, regardless of trsdn being the sole human contributor (gh api .../contributors lists only trsdn and dependabot[bot]) — the exception text requires 'no ruleset'. So B06 remains partial: alerts clean, merge policy unmet. |
| `B13` | partial | Read the standard's B13 boundary directly: Pass = no hand-maintained restatement of a command/version/policy fact; Partial = restatements exist but agree with their home; Fail = copies disagree. I independently re-read the repo files (not just the colleague's evidence): README.md has a "Make targets" table (lines 608-621) listing `make build`, `make test`, `make test-driver`, `make test-all`, `make run`, etc. with hand-typed descriptions; CONTRIBUTING.md "Local validation" (lines 20-25) independently re-describes `make test`, `make test-driver`, `make build`; AGENTS.md (lines 58-80) independently re-describes `make test`, `make test-all`, `make build`, `make run` — none of the three links to the Makefile or to one another as the fact's home, so all count as hand-maintained restatements, not mentions. The Swift 6 toolchain requirement is likewise separately stated in README.md line 49 ("Swift 6 toolchain (for `make test`)") and AGENTS.md line 58 ("Swift 6 toolchain is required for `make test`"). I checked for disagreement and found none: `grep -rn "universal app\/universal binary"` across README/CONTRIBUTING/AGENTS/Makefile now returns only one hit (README.md:47 "universal binary"), and the two APPLE_ID/APPLE_TEAM_ID/APPLE_APP_PASSWORD secrets tables (README.md lines 712-714 and 722-724) agree. docs/self-assessment.md itself records that a prior independent re-check found three disagreements ("universal app" vs "universal binary", a stale "two jobs" claim, a stale CI-secrets table) and that all three were fixed on 2026-09-21 — consistent with what I found in the current tree. I also checked for a reason that could move the result off Partial: no generator produces these tables (grep for "generate/generated/auto-generated" in README/CONTRIBUTING/AGENTS/Makefile shows no doc-generation step for these sections), so the "generated restatement is fine" exception in Content Boundaries does not apply; CONTRIBUTING.md does correctly link to SECURITY.md rather than restate it, which is a mention, not a violation, but does not offset the command/version restatements. No boundary, table reason, or other-means argument moves this to Pass, Fail, or N/A. The colleague's Partial is confirmed. |
| `G06` | partial | Confirmed at commit 00e19c4 (main, HEAD). G06's own boundary text explicitly scopes three path categories — tool-written, copied-in/vendored, and "those a person must not edit, including lockfiles" — each needing marking via .gitignore exclusion, .gitattributes linguist-generated/vendored, or an AGENTS.md name/pattern. Verified: (1) tool-written paths (dist/, build/, .build/, _site/, tools/probe/build/, tools/bench/build/) are marked in .gitignore and/or AGENTS.md's "Generated, never hand-edit" list — met; (2) vendored apm_modules/ is marked in .gitignore — met; (3) tracked lockfiles Update/Package.resolved (git ls-files confirms tracked, last commit b37d38f) and apm.lock.yaml (tracked, committed 2026-09-22 same as HEAD) are unmarked — no .gitattributes file exists at all, and AGENTS.md's 27-word "Generated, never hand-edit" list (line 38) and its do-not-hand-edit rule (line 117) name only dist/, Core/.build/, Update/.build/, tools/*/build/, never Package.resolved or apm.lock.yaml (grep for "resolved"/"lock" in AGENTS.md finds nothing but an unrelated word "sync"). README's two mentions of Update/Package.resolved (lines 689, 769) only state the notarization broker requires it to byte-match its reviewed copy for a dependency bump, and call the AppUpdater bump itself "updated by hand" — this is process documentation, not a do-not-hand-edit marking, so it does not satisfy the criterion. No stated-choice or cheaper-means argument applies: the standard's text pre-empts the "lockfiles are conventionally understood as generated" argument by naming lockfiles explicitly as needing one of the three concrete markings, none of which is present. NA does not apply since qualifying paths do exist. Per the Default Results rule (multi-part criterion: some parts met, some missing = Partial), two of three path categories are marked and one (lockfiles) is not, so Partial is correct — the colleague's result and evidence are confirmed; I found no basis to move it to Pass, Fail, or N/A. |
| `I01` | partial | Independently re-verified by downloading and mounting the actual published v0.5.0 DMG (gh release download v0.5.0 -R trsdn/OpenConnct, hdiutil attach): OpenConnct.app/Contents/Info.plist reads CFBundleName=OpenConnct, CFBundleShortVersionString=0.5.0, CFBundleVersion=66 (matches tag v0.5.0, stamped by scripts/build_release.sh via PlistBuddy from scripts/lib_version.sh's `git describe --tags`). The embedded Contents/Library/Audio/Plug-Ins/HAL/OpenConnct.driver/Contents/Info.plist reads CFBundleName=OpenConnct, CFBundleShortVersionString=1.0.0, CFBundleVersion=1 — confirmed unchanged from the colleague's read. Checked for a rescue and found none: the Makefile's `driver:` target only `cp`s App/OpenConnctDriver/Info.plist verbatim; scripts/make_pkg.sh does call `pkgbuild --version "$VERSION"` for a separately-versioned OpenConnct-driver.pkg, and scripts/make_dmg.sh is written to stage that pkg into the DMG alongside the app, but the actual mounted release DMG contains only OpenConnct.app plus the Applications symlink — no pkg is present, so that correctly-versioned path never reaches the shipped artifact (there is also no `release.yml`/DMG-building workflow in .github/workflows, only ci.yml/pages.yml/stats.yml, consistent with releases being cut by a script outside CI). The release's own provenance.json (from the notarization broker, trsdn/macos-notarization-broker) explicitly lists the driver as a distinct `nested_executables` entry with its own bundle identifier `audio.openconnct.driver` and its own `info_plist_sha256`, but carries no version field for it — it doesn't embed or attest a driver version at all, so it offers no "other means" of meeting I01 for that component. No standard-table reason applies (release v0.5.0 exists, so the "ships nothing → Not applicable" case is moot; this isn't an absent platform capability, a cheaper-but-equivalent substitute, a narrower stated scope, or a documented README/AGENTS.md choice). This is exactly the standard's stated boundary: "A name with a version that differs from the release is Partial" — name matches, version is a hardcoded placeholder that never tracks the tag. The repository's own docs/self-assessment.md (line 15, line 137) and .github/conformance.yml (I01: partial) already recorded this identical finding under 1.15.0, corroborating rather than refuting it. Result: Partial, confirming the colleague. |
| `I02` | fail | Downloaded and mounted the actual shipped v0.5.0 asset (OpenConnct-v0.5.0-macOS-universal.dmg, the "Latest" GitHub release, no newer/draft release exists) myself. OpenConnct.app/Contents/Info.plist contains CFBundleName/Identifier/Version/Executable/DisplayName/IconFile only, no RepositoryURL/IssueTrackerURL or any repo/issue-tracker-naming key; Contents/Resources holds only AppIcon.icns and install-driver.sh (no GitHub URL in it); the bundled HAL driver's own Info.plist likewise has no such key; `strings` on the MacOS/OpenConnct binary shows no github.com/trsdn reference; the DMG root has no loose README/LICENSE carrying the URLs either. The fix (commit 58aea83, "carry repository, licence and copyright in the app, and link them from About") is on main (ancestor of current HEAD 00e19c4) but `git merge-base --is-ancestor v0.5.0 58aea83` confirms it postdates the v0.5.0 tag, so it has not shipped. The standard's "Deciding Without The Maintainer" section states a criterion applies "to the current state of the repository and to its latest published release, not to every release that ever existed," and I02's boundary requires a readable key/file in the artifact itself (unlike I04, it grants no source-only exception for GUI apps). No other-means, intended-deviation-table, or Not-applicable reason fits. Colleague's fail is confirmed. |
| `I03` | fail | Colleague's fail stands; I could not refute it. Latest release remains v0.5.0 (tagged 2026-09-20, gh release list confirms it is still "Latest", assets OpenConnct-v0.5.0-macOS-universal.{dmg,zip} etc.) and its shipped Info.plist has no LicenseIdentifier/NSHumanReadableCopyright, with only AppIcon.icns and install-driver.sh in Contents/Resources (no bundled LICENSE.txt/THIRD-PARTY-LICENSES.txt), matching the colleague's read. The driver's source Info.plist (App/OpenConnctDriver/Info.plist) confirms NSHumanReadableCopyright is literally "OpenConnct", not a holder name. The source-side fix (commit 58aea83a "feat(app): carry repository, licence and copyright in the app...") is confirmed NOT an ancestor of v0.5.0 (`git merge-base --is-ancestor 58aea83 v0.5.0` → not an ancestor; 58aea83 is dated 2026-09-21, a day after the v0.5.0 tag), and no newer release has been published since (HEAD is now 00e19c4, several commits past 58aea83, still no new tag/release). The repo's own docs/self-assessment.md (commit 3f4f5e2, 2026-09-21) already re-checked this exact point and recorded the identical conclusion verbatim: "the source has been fixed ... but the standard reads the published release, so this stays a fail until the next release contains it." Per the standard's "Deciding Without The Maintainer" rule, "A criterion applies to the current state of the repository and to its latest published release, not to every release that ever existed" — so the unreleased source fix cannot lift this above Fail, and no table reason, other-means, or stated-choice exemption applies since an unshipped fix is a real gap, not an intended deviation. LICENSE (MIT, "Copyright (c) 2025 Torsten Mahr") requires notice in copies, confirming the bundled-text requirement the colleague cited. |
| `I06` | partial | Confirmed partial, verified against the live v0.5.0 release rather than just scripts. Downloaded and inspected the actual currently-published OpenConnct-v0.5.0-macOS-universal.zip: the app's CFBundleShortVersionString reads 0.5.0, matching the tag (built via scripts/lib_version.sh's `git describe` + scripts/build_release.sh's PlistBuddy stamp, confirmed reaching the real artifact) and CFBundleVersion reads 66 (differs from source's hardcoded "1", so also machine-produced by some build step, even though undocumented within this repo). But the embedded OpenConnct.driver's Info.plist inside that same shipped app has CFBundleShortVersionString=1.0.0, CFBundleVersion=1, NSHumanReadableCopyright="OpenConnct" — byte-identical to the hand-typed literals in source App/OpenConnctDriver/Info.plist, proving with live artifact evidence (not just source-script grepping) that the driver's fields are never touched by any build step, contrary to README.md's own claim ("The broker stamps the tag's version into the app and driver"). Neither shipped plist carries RepositoryURL/IssueTrackerURL/LicenseIdentifier (those source additions postdate v0.5.0, so they're not yet embedded to judge). Result: at least one identity value is build-produced (app version) and at least three are verifiably hand-maintained and static across a real release (driver's three fields) — the standard's own "some, not all" = Partial boundary applies exactly. No table reason, other-means pass, or narrower scope changes this; the colleague's Partial is correct and now backed by direct artifact inspection. |
| `L05` | partial | Confirmed by direct verification: 52 String(format:) call sites across Theme.swift, EffectPanels.swift, LiveMeters.swift, HeaderBar.swift, MicDetailView.swift, GainCalibrationView.swift and DeviceSelectionView.swift build displayed dB/%/Hz/ms/ppm readouts by hand, and integrations/opendeck/com.trsdn.openconnct.sdPlugin/logic.js:115 does the same with `rounded.toFixed(1)` inside formatDB() -- exactly the toFixed( pattern the standard names as manual formatting. A repo-wide grep for NumberFormatter, .formatted(, FormatStyle, Measurement(, Locale., DateFormatter across every Swift file found zero genuine hits (the one apparent match in NoiseFloorRow.swift is a substring false-positive from 'ActiveMeasurement(', not a Foundation formatter), so no locale-aware API is used anywhere to offset this. No displayed dates or currency exist in the app, and the four .sorted() call sites (ParameterStore.swift:675, DeviceSelectionStore.swift:29, HeaderBar.swift:223, GainCalibration.swift:125) sort UIDs/dictionary keys/numeric arrays, not user-read text, matching the standard's own exclusion. No deviation-table reason applies: NumberFormatter/MeasurementFormatter (incl. UnitFrequency, UnitDuration) are available on macOS, no cheaper equivalent exists, no README/AGENTS.md states a rationale for the fixed-format technical units, and the app's English-only scope does not exempt numeric formatting since decimal-separator conventions depend on system region, not UI language. Under L05's multi-part default (dates/numbers/currency/sorting), dates/currency/sorting have no display instances to violate (vacuously met, the same reading L02 uses for 'none found'), while numbers is comprehensively unmet across both the app and the plugin -- giving Partial (at least one part met, at least one missing), not Fail or Pass. The repo's own docs/self-assessment.md (Gaps table, L05 row) independently reached this identical conclusion in a prior refutation pass. Nothing found refutes the colleague's result; it is confirmed. |
| `L07` | partial | Attempted refutation found none: `gh issue list --repo trsdn/OpenConnct --state all` shows all 13 issues (the full history, well inside the "last twenty") authored by maintainer `trsdn`; issues #8, #9, #10 (fetched via `gh api repos/trsdn/OpenConnct/issues/{8,9,10}`) are entirely in German (title and body, e.g. #8 "Lokale HTTP-API: Kanäle stummschalten und Mixer-Zustand von außen abfragen"), with no English translation attached and not bot/agent output. README.md line 17 ("Primary language: English. The app, its documentation and this site are English only") is a product-language (L01/L03) statement only, states no contributor-language choice, and per the standard's own text the German-Content Exception "covers the product surface only. L07 still applies" — so no stated-choice defense and no exception apply, and the subject matter (an audio-mixer app) is not inherently German so the exception's factual basis is absent anyway. No translated/bilingual version, agent authorship, or other-means evidence exists to raise this to Pass, and 7 of the 8 L07 surfaces (README, docs/, code comments/identifiers, commit messages, PRs, release notes — plus most issues) are English, so "none of it met" (Fail) is also not warranted. This matches the standard's default multi-part rule ("at least one met and at least one missing is Partial"): the colleague's Partial for L07 stands, evidence unchanged plus the confirmation that all 13 issues (not just a window) are maintainer-authored and the exception path is textually foreclosed. |
| `R01` | partial | Confirmed, not refuted. R01's exact boundary: "Pass when every property has a home and the homes agree ...; Partial when every property has a home and one disagrees, or some properties have no home; Fail when none is set anywhere the assessor can read" — and GitHub metadata is the comparison target, not an accepted home (the home must be the manifest or "the artifact's own metadata"). I re-verified the colleague's read of OpenConnct.app/Contents/Info.plist in the downloaded v0.5.0 artifact (name, version present; description/licence/repository URL absent), and went further: pulled the release's own provenance.json and preflight-manifest.json (its other "artifact metadata" files) — neither carries description, licence, or repository URL either, so there is no alternate home in the shipped artifact at all. `git merge-base --is-ancestor 58aea83 v0.5.0` returns false, confirming 58aea83 (2026-09-21) postdates the v0.5.0 tag (2026-09-20) and is genuinely unreleased. Checked whether the fix, once released, would reach Pass: `git show 58aea83` adds RepositoryURL/IssueTrackerURL/LicenseIdentifier to Info.plist but adds no `description`-equivalent field and no README/AGENTS.md statement of "which properties live where" (grep of README.md/AGENTS.md/docs/*.md finds none) — so even the next release stays Partial, not Pass, since `description` would still lack a home. `gh repo view` confirms GitHub carries full description/MIT licence/homepage, which is why 2 of 5 (name, version) having a home and the rest not is "some properties have no home" (Partial per the boundary's exact wording), not "none... anywhere" (Fail). No table reason (platform incapability, cheaper equivalent means, narrower scope, or a stated-and-permitted choice) applies — the gap is a real, undocumented one. Colleague's Partial is correct. |
| `S03` | partial | Confirmed, not refuted. Per the standard's own reading, S03's minimum has two parts — a format-or-lint check, and a type check where the toolchain supports one — and running only one part is Partial. The repo runs the type-check part automatically: .github/workflows/ci.yml's `core` job runs `swift build`/`swift test`, and the `build` job runs `make driver` (clang -Wall -Wextra -Werror on the C HAL driver, Makefile line 57-63) and `make build UNIVERSAL=1` (swiftc for the app); the C++ DSP core compiles with -Wall -Wextra but not -Werror (Makefile line 68-73). No format-or-lint check runs anywhere: repo-wide search finds no .swiftlint.yml/.swift-format, no .clang-format/.clang-tidy, and integrations/opendeck/package.json has only a `test` script (no lint script, no ESLint/Prettier config, no tsconfig.json for a type check on the plain-JS plugin). The ci.yml `naming-consistency` job is a `git grep` for a stray project-name string, not a code-quality check. The newly installed apple-hig-review apm package (apm.yml, apm_modules/, .claude/agents/apple-hig-reviewer.md) is a Claude Code design-review skill, not a lint/format tool, and is not wired into any workflow or Makefile target, so it does not satisfy S03. No documented command (README, CONTRIBUTING.md, AGENTS.md) names a lint/format tool either. This matches "running only part of the minimum... is Partial" exactly, so Partial is correct as given. |
| `X01` | partial | Confirmed via independent source review: App/OpenConnctApp/Views/ChannelStripView.swift's VerticalFader is a custom control replacing a standard Slider, built entirely from DragGesture(minimumDistance:0) (line 91) and onTapGesture(count:2) (line 87, double-click reset to 0dB), with only accessibilityAdjustableAction (line 109) as an equivalent; repo-wide grep confirms zero hits for .focusable(, onKeyPress, and onMoveCommand anywhere in App/, and no keyboardShortcut/keyEquivalent targets fader adjustment. Repo-wide grep for DragGesture/onTapGesture/LongPressGesture/MagnificationGesture found no other gesture-only custom controls outside this one file (the second onTapGesture at line 434, whole-strip tap-to-select, has a redundant standard-Button path via MicTileButton, so selection stays keyboard-reachable). I could not find grounds to improve the result: no automated test or documented manual keyboard check exists (AGENTS.md states there is no screen-rendering command), and the fader is genuinely unreachable by Tab/keyboard outside VoiceOver, so Pass is not warranted. I looked for a reason to downgrade to Fail too and did not find one — I verified that MicDetailView.swift's Slider(s) bind to gainDB (input trim, applied pre-DSP per ChannelSettings.swift:84/AudioEngineRT.swift:72) not faderDB (output mix level, applied post-DSP, ChannelSettings.swift:117/AudioEngineRT.swift:105); these are distinct parameters, so README.md line 793's "same level can be set with the standard slider in the microphone's detail view" is not accurate. Even so, the app's other main-flow actions (select mic, mute, solo, add/remove channel, adjust gain/HPF/gate/compressor) all use standard, keyboard-focusable SwiftUI Buttons/Sliders, so the main flow remains operable and only the fader (plus its double-click reset) is affected, matching the criterion's Partial boundary exactly. This matches the standard's own recorded 1.15.0 result and this repo's docs/self-assessment.md line 26, and X05 (README "Accessibility" section, lines 787-801) already states the fader keyboard gap honestly, so nothing here changes X05 either. |
| `X03` | fail | The colleague's sizing evidence is confirmed exactly (App/OpenConnctApp/Views/Theme.swift:37-40 hardcode labelFont/valueFont 11pt, captionFont 10pt, titleFont 12pt; repo-wide grep for `system(size:` in App/ finds 28 hits, 5 in Theme.swift + 23 inline; grep for ScaledMetric/dynamicTypeSize/.font(.body/.caption/...) returns 0 hits). But the other two properties do not hold either, so the correct result is Fail, not Partial. Contrast: Theme.swift gives literal sRGB token values, which the standard explicitly allows computing from; textSecondary (white 0.55) on raised (0.18/0.18/0.20) — PillToggleStyle's unselected label (Theme.swift:113-126), used in real UI at EffectPanels.swift:225 and MicDetailView.swift:174 with the 11pt labelFont — computes to 4.04:1 (WCAG relative-luminance formula), below the 4.5:1 required for body-sized (non-bold, non-large) text. Colour: SoloButton (ChannelStripView.swift:205-233) conveys its on/off state purely by background/foreground colour (Theme.solo yellow+black text vs Theme.raised grey+grey text), same "headphones" SF Symbol and no border/shape change in either state — the code's own comment confirms this is deliberate ("the glyph does not change with the state, and that is not an oversight"), so this control's own status depends on colour alone. MuteButton's secondary distinction between "you muted it" (Theme.accent/red) and "silenced automatically or by another mic's solo" (Theme.soloDim/amber) is also colour-only, since both render the identical speaker.slash.fill icon (ChannelStripView.swift MuteButton bgColor logic ~lines 138-145); the file's own comment names this ("colour then adds the second question — who silenced it — which a shape cannot express"). Since sizing, contrast, and colour each have a named, confirmed gap in the reviewed source, the standard's rule "Fail where none holds" applies rather than "Partial where at least one holds." |
| `Y01` | partial | Confirmed as partial; I could not find grounds to refute it. Y01's boundary text requires the "collects, stores, or transmits" statement to cover storage, not just collection/transmission — the standard's own example of a Pass sentence is "this application stores all data locally and contacts no network service," explicitly pairing local storage with the network claim. site/index.html:107-109 ("OpenConnct collects nothing... no account, no analytics and no telemetry. The only network request... is a daily check of GitHub for a newer version") and README.md:320 ("That is the only outbound network request the app makes on its own") address collection and transmission only. Source confirms local storage the statement never mentions: App/OpenConnctApp/Persistence/SettingsStore.swift writes per-device channel settings as JSON to AppSupport.directory (~/Library/Application Support/OpenConnct/channels.json), DeviceSelectionStore.swift persists device selection the same way, README.md:327 names a UserDefaults key (updates.automaticChecks.v1), and README.md:340-350 name the api-token and api-port files written to the same directory. No README/site passage states what is stored locally — those facts appear only incidentally in feature-description sections, not as a "what is stored" disclosure. No PRIVACY.md or dedicated data-section exists. This matches Y01's Partial boundary exactly ("statement omits something the source shows"), and does not meet Fail (the "says none and source collects or transmits" Fail case names collect/transmit, not store, and no analytics/telemetry or undocumented network egress was found). Notably, docs/self-assessment.md:28 and :162 and .github/conformance.yml:123 already independently record Y01 as partial for the same reason (local storage read as included under 'stores'), corroborating rather than contradicting the colleague. |
| `Y04` | partial | Confirmed as Partial, but on narrower grounds than the colleague's evidence. OpenConnct writes user data to two distinct location types: (1) the Application Support directory (`AppSupport.swift`: `~/Library/Application Support/OpenConnct/`) and (2) `UserDefaults.standard`. Location (1) is adequately documented for find/delete purposes: README.md:343/353 give the exact path for api-token/api-port, and docs/PHASE1-VERIFICATION.md:457 (itself titled "Phase 1 manual verification runbook" — valid Y04 evidence per the standard's own "README or runbook" evidence type, contrary to the colleague dismissing it) states the whole directory "is safe to delete." That covers `SettingsStore.swift`'s channels.json and `DeviceSelectionStore.swift`'s devices.json too, since they write into the same documented, delete-safe directory — a user does not need every filename named to find or delete them. The real gap is location (2): README.md:331 documents only one UserDefaults key (`updates.automaticChecks.v1`) with no general statement that the app uses UserDefaults for other settings and no bundle identifier given anywhere for a `defaults delete` command. `LocalAPIController.swift:21/33` (API-enabled state) and `AudioEngine.swift:52/77` (latency profile) persist via `UserDefaults.standard` with no documentation at all — a reader has no way to find or delete these. Per the standard's Y04 boundary ("Partial where only some locations are documented"), one of the two storage mechanisms (Application Support) is well documented while the other (UserDefaults) is documented only for one of at least three keys used, which is Partial, not Fail or Pass. |
| `Y06` | partial | Confirmed as partial, not refuted, but on fuller evidence than the colleague cited. Data clearly outlives a session (SettingsStore.swift, a device-selection store, and AudioEngine.swift's UserDefaults latencyProfileKey all persist across launches, plus api-token/api-port in ~/Library/Application Support/OpenConnct/), so Not-applicable is wrong and Y06 must be answered on the merits. Deletion behaviour is partially stated, and by more than the colleague credited: SECURITY.md:18 explicitly tells a user to quit the app and delete ~/Library/Application Support/OpenConnct/api-token to rotate it, and docs/PHASE1-VERIFICATION.md:457 ("installs nothing outside its bundle except a settings file at ~/Library/Application Support/OpenConnct/, which is safe to delete") is a genuine runbook, which the standard's own Y06 evidence column lists as acceptable ("README, runbook, or an explicit not-applicable result") with no user-facing-only qualifier -- so the colleague's stated reason for discounting that document is not correct per the standard's text, even though it doesn't change the final result. It still does not reach Pass: (1) retention itself is never stated anywhere -- no text says these files persist indefinitely with no expiry or auto-cleanup -- and (2) the "delete the directory" guidance is itself incomplete as deletion behaviour, since AudioEngine.swift persists a separate latencyProfileKey in UserDefaults.standard (and README.md:329-331 documents the same pattern for updates.automaticChecks.v1) that deleting the Application Support folder would not remove. site/index.html's Privacy section (checked directly) covers only collection/network (Y01/Y02), nothing about retention or deletion. No README/AGENTS.md/CONTRIBUTING.md statement supplies a scope limitation or other-means equivalent under "Deciding Without The Maintainer." Under Y06's default two-part reading (retention stated, deletion stated), deletion is met only in part and retention is not met at all -- a real, material gap rather than a total absence, which is Partial. The colleague's result stands. |

## Everything else

| Criterion | Result | Evidence |
|---|---|---|
| `A01` | not applicable | gh repo view trsdn/OpenConnct --json isArchived returned {"isArchived":false}. Per the standard, 'A repository whose README says development has ended but which is not archived is not in this profile... A01-A04 are Not applicable with the rationale that it is not archived.' README.md's 'Status and roadmap' section (line 851) describes verified behaviour, known limitations and deferred work consistent with an actively maintained project, not an ended one. |
| `A02` | not applicable | Not applicable for the same reason as A01: gh repo view confirms isArchived:false, so the Archived Repositories profile does not apply to this repository and A02's README-states-why-and-when requirement is not owed. |
| `A03` | not applicable | Not applicable: the repository is not archived (isArchived:false). README.md and the GitHub description ('Run several USB microphones as one system-wide input on macOS...') name no successor or migration destination, and none would be expected for an active project. |
| `A04` | not applicable | Not applicable: the repository is not archived (isArchived:false). Consistent with that, it has an active GitHub Pages deployment (site/ + .github/workflows/pages.yml, https://trsdn.github.io/OpenConnct/), active CI workflows, and a signed/notarized DMG release process -- all normal for an active repository and not evidence of a stray deployment left behind by an ended project. |
| `B01` | pass | gh repo view trsdn/OpenConnct reports description "Run several USB microphones as one system-wide input on macOS: per-channel DSP, drift-corrected mixing, and its own CoreAudio virtual device." — specific, not a restatement of the name or a generic phrase. |
| `B02` | pass | README.md (912 lines) states purpose and audience in the opening paragraphs ("native macOS app for running several USB microphones at once...", "What it is not"), status via the '## Status and roadmap' section (verified capabilities with measurements), setup/usage via '## Install' and '## Using it', and key links: LICENSE badge, CI badge linking to the issue tracker's workflow, and the project site https://trsdn.github.io/OpenConnct/. All five required parts are present. |
| `B03` | pass | LICENSE at repo root contains the full MIT License text ("MIT License / Copyright (c) 2025 Torsten Mahr..."), and gh repo view confirms GitHub recognizes it (licenseInfo.key = "mit"). Explicit granted-permission terms. |
| `B04` | pass | .gitignore covers dist/, build/, *.app, *.driver, *.dmg, *.pkg, .release.env, .build/, .DS_Store, *.xcuserdata, tool-specific build/sample dirs, and apm_modules/ — matching the Swift/Xcode/Node ecosystems in use. `git status --ignored` confirms dist/ is untracked and ignored, not committed. The one credential-adjacent file, App/OpenConnctApp/API/LocalAPICredentials.swift, is source code that generates a random token at runtime (SecRandomCopyBytes) and stores it outside the repo (Application Support, 0600) — it contains no secret value itself. `git ls-files` (204 files) shows no .env, key, or credential file tracked. |
| `B05` | pass | README '## Building and testing' documents `make test` (== `cd Core && swift test`) as the validation command, and CI (.github/workflows/ci.yml) runs it as the 'Swift package' job on every push. `gh run list --workflow=ci.yml --branch main` shows the run for the immediately preceding commit (3f4f5e2, 2026-09-21T11:15:44Z) completed with conclusion "success"; the run for current HEAD (00e19c4) was still in_progress at assessment time with every job so far (Naming consistency, README badges, OpenDeck plugin, and all steps of Swift package/Driver and app run up to that point) green. Evidence: the latest completed run on the default branch is green. |
| `B07` | pass | Core/Package.swift and Update/Package.swift each declare `swift-tools-version: 6.0` and a `platforms:` constraint (macOS 14 for Core, macOS 13 for Update); Update/Package.swift pins its one third-party dependency exactly (`.package(url: "https://github.com/mxcl/AppUpdater", exact: "4.1.2")`); README's Requirements table states macOS 13.0+, Apple Silicon/Intel, and the Swift 6 toolchain; integrations/opendeck/README.md states it runs on whatever Node the deck app provides and explicitly calls out needing a Node newer than Stream Deck's older bundled one (WebSocket/module-detection requirement). Every applicable dependency and runtime is declared. |
| `B08` | pass | CHANGELOG.md has a dated entry for the latest release, '## [0.5.0] - 2026-09-20', whose Added/Fixed content (delay setting, virtual-mic timing-origin fix) matches `gh release view`'s body for tag v0.5.0 (published 2026-09-20T16:30:16Z) almost verbatim. `gh release list` confirms v0.5.0 is the latest release and it has a corresponding changelog entry. |
| `B09` | pass | gh repo view trsdn/OpenConnct --json visibility,repositoryTopics,homepageUrl,isArchived shows visibility PUBLIC, isArchived false, homepageUrl https://trsdn.github.io/OpenConnct/, and 13 topics. README.md line 15 ('Site. https://trsdn.github.io/OpenConnct/ is the project page') names the same homepage, and the repository presents itself throughout as an open-source public project (MIT licence, public issue tracker links) consistent with PUBLIC visibility. No text in README claims maintenance ended, consistent with isArchived:false. All four properties are set and agree with the README. |
| `B10` | pass | No CODEOWNERS file exists anywhere in the repo, but per the standard's own rule the owner for B10 is the account that owns the repository when it is not an organization; trsdn/OpenConnct is owned by the personal account trsdn. `git log -1` shows the latest commit on main is 2026-09-22 (today), which per the standard's default rule ('a commit to the default branch within the review cadence also meets the maintenance-status part') satisfies the maintenance-status part. Both parts present. |
| `B11` | pass | .github/conformance.yml is present with standard_version '1.15.0' and assessed_on '2026-09-21'. The standard's cover states a six-month review cadence; 2026-09-21 is one day before today (2026-09-22), well inside that cadence. B11 asks only that a record exist and be recent, not that its recorded standard_version match the current one, so the record being against an older version does not affect this result. |
| `B12` | pass | gh repo view --json repositoryTopics lists 'trsdn-standard' among the 13 topics on trsdn/OpenConnct, which per the standard marks the repository as governed by the standard regardless of the outcome recorded elsewhere. |
| `B14` | pass | SECURITY.md 'If a credential is exposed' (lines 14-27) names three credential classes and states who acts and how each is revoked and replaced: the local API token (delete the file, restart the app, a new token is created and must be re-entered in every client), the Apple notarization credentials held by the broker (revoke the app-specific password at account.apple.com or the certificate in the Apple Developer account, then put the replacement into the broker's secrets, and re-issue releases signed with a revoked certificate), and a secret committed to the repository by mistake (revoke at its issuer first, then remove it from the tree). This covers revocation and replacement for every credential class the repository names, unlike the stale 'Partial' reading recorded in docs/self-assessment.md's 'Readings applied' section, which does not match the SECURITY.md text as it now reads. |
| `B15` | pass | README.md 'Third-party code' (lines 772-785) states the repository's approach: two Swift packages, AppUpdater 4.1.2 (Unlicense) and Version 2.2.1 (Apache-2.0), are statically linked into OpenConnct.app, both licence texts are bundled in THIRD-PARTY-LICENSES.txt (verified present with the AppUpdater Unlicense text at its top) together with the app's own LICENSE.txt, and the text states explicitly that nothing else is bundled (the OpenDeck plugin and driver use no packages). integrations/opendeck/package.json declares no dependencies, and no node_modules or vendored directory is tracked. |
| `B16` | pass | gh api repos/trsdn/OpenConnct/rulesets/23763420 shows the active ruleset 'protect-main' targets ~DEFAULT_BRANCH and includes rule type 'deletion' and rule type 'non_fast_forward', i.e. both deletion and force-push are blocked. Classic branch protection on main returns 404 ('Branch not protected'), but the standard treats a ruleset and classic protection as equally acceptable mechanisms, and the ruleset alone satisfies both parts. |
| `D01` | not applicable | OpenConnct is a macOS app users download from a GitHub Release (signed, notarized DMG) and drag to /Applications, per README.md 'Install' section (lines ~48-100). It matches the standard's Deployable-profile decision table row 'An application or tool that users, the maintainer included, download or build and install from a release, such as a macOS app or a CLI → No: the Package profile decides it.' There is no service, container, launch agent, daemon, or scheduled job the maintainer installs and keeps running: README.md line 90 states explicitly 'There is no helper daemon and no persistent elevated component.' The repository's own docs/self-assessment.md records the same reading for D01. |
| `D02` | not applicable | Same basis as D01: OpenConnct is a downloaded/installed macOS app, not a standing deployment the maintainer operates, so D02 (deployment secrets) is not applicable. The local HTTP API (README.md line 336-342) is off by default, loopback-only (127.0.0.1), and runs only inside the user's own app process — it is not a deployment secret concern under this criterion. |
| `D03` | not applicable | Same basis: no standing deployment exists for this repository to health-check or roll back. Release-level verification (equivalent concern) is instead covered by scripts/smoke_release.sh and the README's 'Verifying a download' section, but that is Package/Release territory (R-series), not Deployable. |
| `D04` | not applicable | Same basis: no server, container, or infrastructure the maintainer runs. The GitHub Pages site (site/ + .github/workflows/pages.yml, published at https://trsdn.github.io/OpenConnct/) is a static host with no server the maintainer operates, which the standard's decision table assigns to the Published Site profile, not Deployable ('A site served by GitHub Pages ... with no server the maintainer runs → No: the Published Site profile decides it'). |
| `D05` | not applicable | Same basis: with no standing deployment, there is no deployment-configuration change to track in durable history/inventory under D05. Ordinary code changes are tracked via CHANGELOG.md, which is assessed elsewhere (e.g. B-series), not as a D05 deployment inventory. |
| `D06` | not applicable | Same basis: the repository operates no host holding state (database, uploaded files, host-only configuration) since there is no standing deployment — the app's own user-machine state (e.g. locally stored driver files, API token) is local to the end user's install, not a maintainer-operated deployment. D06 is therefore Not applicable rather than merely 'not stateful'. |
| `G01` | pass | AGENTS.md exists at the repository root (6.3 KB, tool-neutral filename), confirmed on HEAD (commit 00e19c4, main). It opens with 'Read this before changing anything in this repository' and is the only top-level agent-instruction file. |
| `G02` | pass | AGENTS.md states purpose ('What this repository is'/'What this repository is not'), a Layout table of every top-level path, a Setup section (Xcode CLT, Swift 6 toolchain), a Run section ('make run'), and names 'make test-all' as 'the single command that must succeed' to validate a change. Makefile:176 defines 'test-all: test test-driver'. Per the read-only assessment constraint the command was not executed locally; the linked evidence is CI workflow .github/workflows/ci.yml, whose 'core' job runs 'swift test' and whose 'build' job runs 'make test-driver' (the two constituents of test-all) — 'gh run list' shows the CI workflow completed 'success' on main at commit 3f4f5e2 (2026-09-21T11:15:44Z); 'git diff --stat 3f4f5e2 00e19c4' shows the only changes since then are .claude/ files, apm.yml/apm.lock.yaml and a 9-line AGENTS.md addition, no Swift/driver code, so that green run is representative of current HEAD. |
| `G03` | pass | AGENTS.md 'Do not do these' names: history rewriting/force push/branch deletion; committing secrets, tokens, credentials, or personal data; deploying, publishing a release, creating/moving tags, or changing repository settings without being asked; and running 'make install-driver'/'uninstall-driver' or any sudo script under scripts/ because they install a HAL plug-in into a running coreaudiod and can affect the system's audio outside the repo. |
| `G04` | pass | None of the tool-specific files the standard lists exist: no .github/copilot-instructions.md, CLAUDE.md, GEMINI.md, .cursorrules, .cursor/rules/, .windsurfrules, or .clinerules (checked directly, all absent). .claude/agents/, .claude/commands/, .claude/rules/ exist (from the apm-installed apple-hig-review package) but are not in the standard's G04 list of tool-specific instruction files, so they do not create a divergence to assess. Per the standard's own text, where none of the listed files exists the result is Pass because there is nothing to diverge. |
| `G05` | pass | AGENTS.md names 'make test-all' as the single command B05/G05 point to, matching Makefile:176 ('test-all: test test-driver'). Evidence is the same green CI run used for G02 (workflow 'CI' success on main at commit 3f4f5e2, 2026-09-21T11:15:44Z, running swift test and make test-driver), which post-dates only non-functional commits (apm package install, AGENTS.md addition) relative to current HEAD 00e19c4. |
| `G07` | pass | AGENTS.md 'Attribution' section states: 'Agent-authored changes are identified by the Co-Authored-By / session trailers their tool adds to commit messages, and are reviewed the same as any other change before merging to main' — a documented trailer rule plus review expectation. 'git log' on the two most recent non-dependabot commits (00e19c4, 3f4f5e2) confirms the practice matches: both carry 'Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>' and a 'Claude-Session:' trailer. |
| `G08` | pass | .github/github-app.yml is committed, non-empty (523 bytes), and reads: 'The authoritative instructions for this repository are in AGENTS.md. This file points at them rather than restating them' plus an 'instructions:' block telling the agent to read AGENTS.md and a 'scripts: - name: Validate, command: make test-all' entry that matches the Makefile's test-all target — no contradiction with AGENTS.md. 'gh api repos/trsdn/OpenConnct' confirms the repository is hosted on GitHub, public, and not archived, so the platform capability applies and is used. |
| `I04` | pass | Refuting the colleague: App/OpenConnctApp/Info.plist (committed on main, HEAD 00e19c4, added in commit 58aea83 "carry repository, licence and copyright in the app") does contain RepositoryURL (https://github.com/trsdn/OpenConnct), IssueTrackerURL (.../issues) and LicenseIdentifier (MIT) alongside CFBundleShortVersionString=1.0.0 — the colleague's claim that these keys are "absent from the actually-shipped Info.plist" and that the version is 0.5.0 does not match the current repository. A locally built app bundle, dist/OpenConnct.app/Contents/Info.plist, independently confirms all three custom keys plus CFBundleShortVersionString=1.0.0 are actually present in a real built artifact, not just intended in source. AboutCommand.swift reads exactly these three Bundle.main.infoDictionary keys and renders them as clickable links plus licence text in the standard About panel's credits view (NSApp.orderFrontStandardAboutPanel), which macOS also shows the version in (per the standard's own note that this is what the panel does automatically). Per I04's boundary for a GUI app the assessor need only confirm the source renders version and both links without operating the app, which is satisfied, and the artifact-level check goes further and corroborates it, so this is Pass, not Partial. |
| `I05` | pass | The actual v0.5.0 app bundle embeds Contents/Resources/AppIcon.icns (CFBundleIconFile=AppIcon), generated by scripts/make-icon.py from one programmatic design (three channel-strip faders on a teal-to-blue squircle). site/assets/icon.png (256×256, used as the Pages site favicon and header image, site/index.html lines 9 and 16) is a resized export of the same App/OpenConnctApp/Resources/AppIcon.png design (visually identical: same colours, gradient and fader layout, verified by direct image comparison). Only the two surfaces the repo actually has — the app and the GitHub Pages site — were assessed; there is no store listing. |
| `L01` | pass | README.md line 17 states in one sentence: "Language. Primary language: English. The app, its documentation and this site are English only; there are no other locales." This is the exact one-sentence declaration form the standard asks for. |
| `L02` | pass | Searched every Swift, C, C++, JS, HTML, Markdown and JSON file under App/, Core/, integrations/, site/, scripts/ and docs/ (excluding apm_modules and .git) for non-ASCII characters with `grep -lP "[^\x00-\x7F]"`; zero files matched. A separate umlaut-specific search of the 15 SwiftUI view files with user-facing Text/Label calls also found none. No hardcoded non-English strings found in a sample far larger than the 20-file minimum. |
| `L03` | pass | The same README sentence ("...English only; there are no other locales.") explicitly declares English-only support, and no `.lproj`, `.strings`, `.stringsdict`, `.xcstrings`, `locales/`, or i18n config files exist anywhere in the repository (build-cache references to a system `MacLocales.h` header are unrelated). One sentence carries both L01 and L03 as the standard allows. |
| `L04` | not applicable | The repository ships no locale catalogs at all (confirmed under L03: no .strings/.stringsdict/.xcstrings/locales files exist) and declares English-only, so there are no "localized builds" shipping more than one locale for this criterion to apply to. |
| `L06` | not applicable | The repository declares English-only under L03 and ships no translated catalogs of any kind, so there is nothing translated for a translation-traceability requirement to apply to. |
| `P01` | pass | Root LICENSE (MIT License, Copyright 2025 Torsten Mahr) is present, and `gh api repos/trsdn/OpenConnct --jq .license` reports {key: "mit", spdx_id: "MIT"} — GitHub detects an OSI-approved SPDX identifier. |
| `P02` | pass | Both CONTRIBUTING.md (repo-specific workflow: local validation steps, when to open an issue first, pointers to SECURITY.md) and CODE_OF_CONDUCT.md (Contributor Covenant, full text with Our Pledge/Our Standards/enforcement) are present at the repository root, and `gh api repos/trsdn/OpenConnct/community/profile` confirms GitHub recognizes both files. |
| `P03` | pass | SECURITY.md documents a private reporting route (GitHub Security Advisories link, 'Do not open a public issue', 7-day/30-day response targets) and `gh api repos/trsdn/OpenConnct/private-vulnerability-reporting` returns {"enabled":true}, so both the documented policy and the actual GitHub setting are present. |
| `P04` | pass | Two issue forms exist in .github/ISSUE_TEMPLATE/ (bug_report.yml, feature_request.yml) plus config.yml with blank_issues_enabled:false and contact links, and .github/PULL_REQUEST_TEMPLATE.md exists with a structured checklist (type of change, validation, realtime safety, plug-in safety, measurements, documentation). GitHub's community profile shows health_percentage:100, consistent with both being recognized despite the API's 'issue_template' summary field returning null for directory-based multi-form setups (a known API display quirk, not an absence). |
| `P05` | pass | README.md covers all six topics: Install (## Install, ## Requirements), configuration (## Using it, per-channel controls table), examples (numbered walkthrough under ## Using it, Controlling it from other apps), compatibility (## Requirements: macOS 13.0+, arm64/x86_64), security (## Verifying a download, references to SECURITY.md, permissions section), and support status (## Status and roadmap: 'What is verified', 'Known limitations', 'Known deferred work' with measured evidence). |
| `P06` | pass | `gh api repos/trsdn/OpenConnct/community/profile` reports health_percentage 100 and lists all four files as recognized: readme, license (MIT, root LICENSE), contributing (CONTRIBUTING.md), and code_of_conduct (Contributor Covenant, CODE_OF_CONDUCT.md). |
| `P07` | pass | `gh api repos/trsdn/OpenConnct` shows a non-empty description ('Run several USB microphones as one system-wide input on macOS: per-channel DSP, drift-corrected mixing, and its own CoreAudio virtual device.'), 13 topics including 'trsdn-standard', and homepage https://trsdn.github.io/OpenConnct/ which returns HTTP 200 with a populated <title> (verified by direct fetch). |
| `P08` | pass | README.md badges (lines 3-7) are, in order: license (shields.io github/license/trsdn/OpenConnct, dynamic, links to LICENSE), platform (static 'macOS-13+' badge linking to #requirements), CI (github actions workflow badge for ci.yml, live), latest release (shields.io github/v/release, dynamic, links to releases/latest), conformance (.github/badges/conformance.svg, committed, links to .github/conformance.yml). The platform badge is hardcoded but scripts/check_badges.sh compares it against Makefile's DEPLOY_TARGET=13.0 and runs as the 'readme-badges' job in .github/workflows/ci.yml (last completed run on main succeeded); the same script also checks the committed conformance badge's text against .github/conformance.yml's state field ('Needs work' matches the SVG). No extra unexplained badges are present. |
| `P09` | pass | .github/workflows/stats.yml runs a daily schedule (cron '23 5 * * *') plus workflow_dispatch, calling the reusable trsdn/.github repo-stats workflow with theme:both, committing repo-card.svg and repo-card-dark.svg to the 'stats' branch (contents:write scoped to that job/branch, not main). README.md lines 901-906 render both via a <picture>/<source prefers-color-scheme:dark> element referencing the stats branch raw URLs. Fetched origin/stats:.github/stats/repo-card.svg and repo-card-dark.svg: both are self-contained (only external string found is the SVG XML namespace, no remote fonts or <image href>) and carry a footer timestamp 'auto-generated · 2026-09-21 07:31 UTC' consistent with the daily schedule. |
| `P10` | pass | .github/ISSUE_TEMPLATE/bug_report.yml (a form template, config.yml sets blank_issues_enabled:false) has required fields 'What happened' (observed vs expected), 'Steps to reproduce', a 'Which part' dropdown, 'macOS version and architecture' and 'OpenConnct version' (both required), and 'Microphones and interfaces'. This covers problem, expected/actual result, reproduction steps, and version/environment. |
| `P11` | pass | .github/PULL_REQUEST_TEMPLATE.md has 'Summary' (what changes and why, with an issue-link prompt for what it relates to), 'Validation' (make test / make test-driver / make build / hardware check for how it was validated), and 'Realtime safety' plus 'Plug-in safety' checklists (each requires 'Not affected' if unused) that make the contributor state risk to the audio callback and HAL plug-in, the project's two highest-risk areas. |
| `P12` | pass | gh api repos/trsdn/OpenConnct/vulnerability-alerts returned HTTP 204 (enabled). gh api repos/trsdn/OpenConnct/automated-security-fixes returned {"enabled":true,"paused":false}. Both platform settings are on. |
| `P13` | pass | gh api repos/trsdn/OpenConnct/code-scanning/default-setup returns state:'configured', languages: actions, c-cpp, javascript, javascript-typescript, python, swift, typescript — covering the repo's Swift app, C HAL driver, C++ DSP core and the OpenDeck JS/TS plugin. No repository-owned CodeQL workflow exists (.github/workflows only has ci.yml, pages.yml, stats.yml), consistent with default setup being the active mechanism. gh api code-scanning/analyses shows recent analyses (2026-09-22) for c-cpp, javascript-typescript, python and actions on the current HEAD commit 00e19c4, and for swift on a prior main commit, all with empty 'error' fields (no run failures). |
| `R02` | pass | README.md, section 'Versioning' (after 'Releasing'): 'OpenConnct uses Semantic Versioning. While the major version is 0, a minor release (0.x.0) may change behaviour, including the local API under /v1 and the settings, and the changelog says so. A patch release (0.x.y) only fixes defects.' This names SemVer and states what a consumer can rely on across versions. |
| `R03` | pass | README.md, 'Releasing' section documents the exact procedure: `git tag -a vX.Y.Z -m ... && git push origin vX.Y.Z`, then `scripts/request.sh openconnct vX.Y.Z --publish` run from a checkout of the shared pipeline trsdn/macos-notarization-broker. `git tag --sort=-creatordate` confirms v0.5.0 exists and resolves to commit ef5281a (verified via `git log -1 v0.5.0`); the broker's provenance.json (named in README) records the built source commit. This is the documented 'shared release pipeline a maintainer starts for a specific tag' form the standard names as qualifying. |
| `R04` | pass | `gh release list` shows the latest release tag=v0.5.0, title='v0.5.0'. Downloading and inspecting the actual released app's Info.plist gives CFBundleShortVersionString=0.5.0. All three normalize to 0.5.0 and agree. |
| `R05` | pass | scripts/smoke_release.sh (75 lines) downloads the published release DMG via `gh`, checks the sha256, byte-identity of the updater filename copy, `xcrun stapler validate`, `codesign --verify --deep --strict`, `spctl -a -t exec` (must say Notarized Developer ID), version match, and driver/app same-team signing. README records: 'Smoke test record (an agent ran it, not the maintainer): v0.5.0 / 2026-09-21 / pass, all seven checks.' `git diff v0.5.0..HEAD --stat` on the build/sign/package scripts (build_release.sh, make_pkg.sh, make_dmg.sh, notarize_pkg.sh, notarize_dmg.sh, lib_version.sh) shows no changes since that release, so the record is current. I independently re-verified by downloading the v0.5.0 zip and running `codesign --verify --deep --strict` (exit 0) and `spctl -a -t exec -vv` (accepted, source=Notarized Developer ID, Torsten Mahr G69Z5BNY97). |
| `R06` | pass | CHANGELOG.md '## [0.5.0] - 2026-09-20' names specific user-visible changes: a new Delay setting with measured latency figures per mode, and a fix for a random up-to-340ms delay bug in the virtual microphone, including that the driver must be reinstalled and restarts system audio briefly. `gh release view v0.5.0 --json body` shows the release notes are this exact text. No breaking change or minimum-requirement increase occurred in this release, so no warning was owed. |
| `R07` | pass | `gh release view v0.5.0 --json body` output is character-for-character the '## [0.5.0] - 2026-09-20' section of CHANGELOG.md. README documents the gate: the notarization broker 'publishes that entry as the release notes and refuses to publish a version that has none, or while anything is still listed under Unreleased', so the connection is enforced by the shared pipeline rather than by hand. |
| `R08` | pass | README 'Verifying a download' documents the exact mechanism: Apple notarization/Developer ID signature checkable via `codesign --verify --deep --strict` and `spctl -a -t exec`, plus sha256 checksums and provenance.json from the notarization broker. This is the model case the standard names explicitly for a macOS app released through a notarization broker. I reproduced it myself on the downloaded v0.5.0 artifact: `codesign --verify --deep --strict` exited 0, and `spctl -a -t exec -vv` returned 'accepted / source=Notarized Developer ID / Torsten Mahr (G69Z5BNY97)'. |
| `R09` | pass | `gh api repos/trsdn/OpenConnct` .security_and_analysis shows secret_scanning=enabled, secret_scanning_push_protection=enabled, dependabot_security_updates=enabled. `gh api repos/trsdn/OpenConnct/secret-scanning/alerts` returns `[]` (0 open alerts) and `gh api -i repos/trsdn/OpenConnct/vulnerability-alerts` returns 204 (Dependabot alerts enabled) with `gh api repos/trsdn/OpenConnct/dependabot/alerts` returning `[]` (0 open alerts). Both native checks the criterion names directly (GitHub secret scanning with no open alert, Dependabot alerts with no open critical/high advisory) currently hold clean. |
| `S01` | pass | Core/Package.swift declares zero third-party dependencies (only internal targets), so no lockfile is needed there; Update/Package.swift pins AppUpdater to an exact version (`exact: "4.1.2"`) and Update/Package.resolved is committed as the lockfile. integrations/opendeck/package.json declares no dependencies (Node built-ins only). README 'Building and testing > Prerequisites' documents `xcode-select --install` plus the Swift 6 toolchain, and 'Make targets' documents every command (`make build`, `make test`, etc.) needed for a clean-checkout build; these were not re-run but are read as reproducible from a clean checkout. |
| `S02` | pass | Core/Tests/ (3400 lines) exercises the DSP/control core that is the app's main product function (biquad HPF, noise gate, compressor, gain/noise-floor calibration) and includes failure/refusal paths, e.g. GainCalibrationTests.swift:229 asserts a gain request beyond the ceiling 'should refuse'. integrations/opendeck test/smoke.js exercises a 401 unauthorized path (line 64) for a wrong bearer token. tools/driver_harness drives the HAL driver's vtable directly (make test-driver). CI run 35593119177 (2026-09-21) shows all 5 jobs green including 'Swift package' (`swift test`) and 'Driver and app' (`make test-driver`), confirmed via `gh run view --json jobs`. |
| `S04` | pass | README 'Requirements' claims macOS 13.0+ (a range, 'Ventura+') and a universal arm64+x86_64 binary. The 'Driver and app' and 'Swift package' CI jobs run on `runs-on: macos-latest` (the newest hosted macOS runner), and the build step runs `make build UNIVERSAL=1`, which compiles both the arm64 and x86_64 slices and lipos them together, so both claimed architectures are exercised in CI. The OpenDeck plugin makes no version-range claim beyond 'whatever Node the deck app provides' and is tested on Node 20 (the oldest Stream Deck-supplied Node, per ci.yml comment), which is the strictest available target. |
| `S05` | pass | `gh api repos/trsdn/OpenConnct` shows `security_and_analysis.secret_scanning.status: "enabled"` and `secret_scanning_push_protection.status: "enabled"` on this public repository — native GitHub secret scanning with push protection running on commits and pull requests. |
| `S06` | pass | The local API token is generated at runtime from SecRandomCopyBytes and never hardcoded (App/OpenConnctApp/API/LocalAPICredentials.swift:17-35); a failed RNG hard-fails via preconditionFailure rather than falling back to a guessable value. Release signing/notarization values are read from environment/`.release.env` (scripts/build_release.sh, notarize_dmg.sh, etc. via `${VAR:-}`), and `.release.env` is listed in .gitignore so the real file is never committed — only `.release.env.example` (containing the maintainer's own public Developer ID team info, not a secret) is tracked. No home-directory paths or credential-shaped strings were found hardcoded as defaults in App/, Core/, or integrations/. |
| `S07` | pass | NSLog calls in App/OpenConnctApp/API/LocalAPIServer.swift and Audio/AudioEngine.swift name the failed operation and its cause, e.g. LocalAPIServer.swift:57 'local API could not create a listener: %@' and AudioEngine.swift:101 'could not set buffer size %u on device %u (status %d)'. A repo-wide search for logging of tokens, headers, or passwords near NSLog/print/console.log call sites found none; the API token itself is never logged. |
| `S08` | pass | /Volumes/big/dev/OpenConnct/.github/dependabot.yml runs weekly for the github-actions ecosystem; its header comment explains AppUpdater is intentionally excluded because the notarization broker requires Update/Package.resolved to match its own reviewed copy, so that bump is deliberate. README.md 'Dependency updates' (line ~766) states "The maintainer owns dependency updates and reads the advisories for what the app links" and names the manual process for AppUpdater. Core/Package.swift declares no third-party dependencies and integrations/opendeck/package.json declares none either, so the only actual third-party runtime dependencies (AppUpdater, Version) are covered by this documented owner/process. |
| `S09` | pass | `gh api repos/trsdn/OpenConnct/rulesets/23763420` shows ruleset "protect-main" (target: branch, enforcement: active, ref include: ~DEFAULT_BRANCH) with a required_status_checks rule listing five contexts: "Naming consistency", "README badges", "OpenDeck plugin", "Swift package", "Driver and app". These match exactly the five job `name:` values in .github/workflows/ci.yml (naming-consistency, readme-badges, opendeck-plugin, core, build), so all five are existing, exercised checks, not placeholders. |
| `S10` | pass | README.md has a dedicated "Architecture" section (lines 402-591) with subsections "Why not an aggregate device?", "The mirror-device design", "Why the driver is dumb", "Signal path", "Gain", "Where each control runs", "Stages the host cannot see", "Clock drift correction", "Realtime safety", and "Repository layout" naming the app/driver/DSP-core components and constraints a contributor could otherwise break (e.g. the driver must stay logic-free, realtime-safe code must not allocate/lock). docs/device-control.md documents the local HTTP API surface additionally. |
| `S11` | pass | .github/workflows/ci.yml declares a single workflow-level `permissions: contents: read` covering all five jobs, each of which only checks out code and runs build/test commands. .github/workflows/pages.yml declares workflow-level `contents: read` and overrides only the `deploy` job with `pages: write` and `id-token: write`, which actions/deploy-pages@v5 requires. .github/workflows/stats.yml declares workflow-level `contents: write`, needed because its single job pushes the generated stats card to the `stats` branch. No workflow omits a permissions block and none grants a scope its steps do not use. |
| `S12` | pass | All external action references across ci.yml and pages.yml are GitHub-published (actions/checkout@v7, actions/setup-node@v7, actions/configure-pages@v6, actions/upload-pages-artifact@v5, actions/deploy-pages@v5), for which a major-version tag is sufficient per the standard's table. The only reusable-workflow reference, `trsdn/.github/.github/workflows/repo-stats.yml@main` in stats.yml, targets an account-owned repository (trsdn), so a branch reference (including the default branch) is explicitly permitted. No third-party (non-GitHub, outside-account) action is referenced by a moving tag in a job holding a secret or write token. |
| `S13` | not applicable | `grep -rn "pull_request_target\/workflow_run" .github/workflows/*.yml` returns no matches: ci.yml triggers only on `push`/`pull_request` (the default `pull_request` trigger runs with a read-only, secret-less token for fork PRs), pages.yml triggers on push-to-main/schedule/workflow_dispatch, and stats.yml triggers on workflow_dispatch/schedule/push-to-main. No workflow uses the trigger types this criterion is about, so the repository has nothing for it to assess. |
| `T01` | not applicable | OpenConnct's primary product is a macOS app (Swift app in App/, C HAL driver, C++ DSP core in Core/), not documentation, research, content, or templates, so the Documentation profile (Profiles table: 'Its primary product is documentation, research, content, or templates') does not apply and the Documentation Repositories section (T01-T05) does not reach this repository. docs/ contains only three supporting files (device-control.md, PHASE1-VERIFICATION.md, self-assessment.md) alongside the app's own README.md, CONTRIBUTING.md, SECURITY.md — supporting material for a software product, not the product itself. |
| `T02` | not applicable | Same basis as T01: the Documentation profile does not apply to OpenConnct (a Swift/C/C++ macOS app whose primary product is the application, not documentation), so T02's link/output-validation requirement, scoped to the Documentation Repositories profile, does not reach this repository. |
| `T03` | not applicable | Same basis as T01: the Documentation profile does not apply to OpenConnct, so T03's sources-vs-conclusions requirement, scoped to the Documentation Repositories profile, does not reach this repository. |
| `T04` | not applicable | Same basis as T01: the Documentation profile does not apply to OpenConnct, so T04's generated-artifact-provenance requirement, scoped to the Documentation Repositories profile, does not reach this repository. |
| `T05` | not applicable | Same basis as T01: the Documentation profile does not apply to OpenConnct, so T05's stale/superseded-material requirement, scoped to the Documentation Repositories profile, does not reach this repository. |
| `W01` | pass | site/ is committed source and .github/workflows/pages.yml builds it via scripts/build_site.sh and deploys through actions/deploy-pages (gh api repos/trsdn/OpenConnct/pages confirms build_type 'workflow', source branch 'main'); README.md:15 states 'Site. https://trsdn.github.io/OpenConnct/ is the project page, built from site/ in this repository', documenting the process. |
| `W02` | pass | gh api repos/trsdn/OpenConnct shows homepage = https://trsdn.github.io/OpenConnct/, meeting the repository half. site/index.html has a persistent header link 'Source on GitHub' (line 19) and footer nav link 'Source code' (line 116), both to https://github.com/trsdn/OpenConnct, on the site's only page, meeting the site half. |
| `W03` | pass | In site/index.html the only content before the hero <section> is the <header class="top"> navigation (brand + source link); the hero then gives h1 'Two USB microphones, one input.' plus a lead paragraph explaining it mixes USB mics into one input selectable in Teams/Zoom/OBS (what it is and, via that use-case, who it's for), and a .status line 'Maintained. Needs macOS 13 or later.' (status word), all in source order ahead of any other content. |
| `W04` | pass | All seven baseline items are present on site/index.html: (1) h1 + lead sentence naming the project; (2) status word 'Maintained' plus download buttons linking to /releases/latest, naming the release; (3) 'What you get' features list and a mixer screenshot; (4) 'Get it' section with numbered install steps and a download button; (5) 'Privacy' section, one sentence disclosing nothing is collected; (6) footer nav links to source, MIT license, SECURITY.md, and issues (support); (7) footer 'Page last reviewed {{DATE}}.' with scripts/build_site.sh substituting the real ISO date at build time and failing the build if any '{{' placeholder remains unfilled. |
| `W05` | not applicable | Retired 2026-09-17 in standard 1.12.0 (superseded by W09 per decision 0013); recorded as Not applicable with the fixed rationale 'retired'. |
| `W06` | not applicable | Retired 2026-09-17 in standard 1.12.0 (superseded by W09 per decision 0013); recorded as Not applicable with the fixed rationale 'retired'. |
| `W07` | pass | grep of every src=/href=/@import/url()/<script> in site/index.html and site/style.css shows only same-origin paths (assets/icon.png, assets/mixer.png, style.css, #fragment anchors) plus outbound links to github.com that a visitor follows (source, releases, license, security, issues) — no third-party loaded resource, no <script> tag, and no cookie or analytics code found; the Privacy section itself states 'This page sets no cookies and loads nothing from other sites.' |
| `W08` | pass | site/index.html has no contributor, architecture, or changelog section (sections are hero, why-drift-correction, features, get-it, privacy only). Brief overlaps with README (e.g. 'signed... and notarized' on the site vs. README's detailed signing/notarization internals, or the simplified 'Get it' steps vs. README's full Install section) are user-facing summaries of content the baseline (W04) explicitly requires on the site, not verbatim technical facts duplicated without a home. |
| `W09` | pass | site/style.css opens with 'Colours are the app's own (App/OpenConnctApp/Views/Theme.swift), so the page and the product read as one thing' and defines a custom :root color palette (--ink, --panel, --button #d0342c, etc.) with a distinct light-scheme override, a custom type scale using ui-rounded/SF Pro Rounded for headings, bespoke two-column grid layouts per section, and a hand-built animated SVG clock-drift illustration — not a framework or template default and not another project's site reused unchanged. |
| `X02` | pass | Every interactive control found in App/OpenConnctApp/Views carries an accessible name: the custom fader has accessibilityLabel("Fader") plus a value (ChannelStripView.swift lines 106-109), the Mute and Solo icon-only buttons carry accessibilityLabel/accessibilityValue (lines 159-160, 222-223), and Open Settings, Remove microphone and Add microphone buttons are similarly labelled (lines 279, 312, 479); Theme.swift's Sliders and standard SwiftUI controls supply name and role by default. site/index.html gives the screenshot figure a role=img with aria-label. Assessment is source review only, per AGENTS.md's note that there is no rendering command. |
| `X04` | not applicable | The product ships a GUI app, a local HTTP JSON API, an OpenDeck plugin, and a GitHub Pages site — none is a terminal/CLI surface with formatted output. The only text-output pieces in the repo are tools/*.swift and scripts/*.sh developer probes; grepping them for NO_COLOR, isatty, or ANSI escape sequences finds none, and they are not a product-facing CLI. The criterion is Not applicable where the product has no terminal output, which is the case here. |
| `X05` | pass | README.md "Accessibility" section (line 787 onward) states exactly the limitations a source review finds: fader not keyboard-focusable (matches X01's DragGesture/onTapGesture finding), fixed text sizes not following Dynamic Type (matches X03's Font.system(size:) finding, confirmed by 28 fixed-size uses), meters hidden from VoiceOver (confirmed: App/OpenConnctApp/Views/LiveMeters.swift lines 172/230/258 use accessibilityHidden(true)), the double-click fader reset having no keyboard/VoiceOver equivalent, the interface being always dark, and numbers using fixed decimal formatting. It also states icon-only buttons carry VoiceOver labels and that this was checked by source review, not screen-reader testing. No limitation found under X01-X03 is left unstated, so the statement is complete. |
| `Y02` | pass | README.md 'Checking for updates' (lines 316-320) documents the app's only outbound destination: a daily check of GitHub releases via AppUpdater, stated purpose 'to check for a newer version', with a following paragraph explaining what the check downloads and does. No URLSession/URLRequest calls exist anywhere in App/Core/Update/integrations source outside the AppUpdater dependency, and the local HTTP API (127.0.0.1 only, README.md:342) is inbound-listening, not an outbound destination the standard counts. No undocumented destination was found. |
| `Y03` | pass | No telemetry, analytics, or crash-reporting library or code was found: Core/Package.swift and Update/Package.swift declare only OpenConnctDSP/OpenConnctControl (in-repo) and AppUpdater (update checking) as dependencies; a repo-wide search for telemetry/analytics/Sentry/Crashlytics/Firebase/Bugsnag/Mixpanel/Amplitude names returned no hits in product source. The one network behaviour present, the daily update check, is disclosed in README.md and site/index.html and can be turned off via the 'Check for Updates Automatically' menu item (README.md:330-331). |
| `Y05` | pass | No source sends user content to any third party or AI provider. The app's only outbound contact is the documented GitHub release/version check (README.md:316-320, site/index.html:109), which transmits no user content -- it checks for a newer release only. No AI-provider SDKs, HTTP clients to third-party APIs, or content-uploading code were found in App/Core/Update/integrations. |

## Readings applied

Where a criterion uses a judgement word, the reading used is stated here so it can be checked.

- `B05`: Assessor did not execute build/test commands locally (read-only constraint on this run); relied on the documented command plus the latest completed green CI run on main, per B05's own 'green run on the default branch' allowance.
- `B06`: The single-maintainer/no-ruleset default exception for 'all three methods enabled with nothing stated' does not apply here because a ruleset (protect-main) does exist on the default branch, even though that ruleset does not itself require a pull request — read literally per the standard's 'and no ruleset' wording, so the merge-policy sub-part is a real gap rather than an intended deviation.
- `D01`: Not a standing deployment (Deployable does not apply); recorded per the standard's required sentence 'not a standing deployment', naming the 'application ... installed from a release' row as the match. The Package profile (R01-R08) covers this repository instead.
- `D02`: Not a standing deployment; recorded 'not a standing deployment', matching the 'application ... installed from a release' row.
- `D03`: Not a standing deployment; recorded 'not a standing deployment'.
- `D04`: Not a standing deployment; recorded 'not a standing deployment', naming the Pages-site row as the closest non-match (still Not applicable to D04).
- `D05`: Not a standing deployment; recorded 'not a standing deployment'.
- `D06`: Not a standing deployment, so D06 does not reach the statefulness question at all; recorded 'not a standing deployment'.
- `G03`: The repository has no database or user-data store to 'destroy', so its nearest equivalent to 'data-destructive commands' is the sudo driver install/uninstall path that can affect live system audio; that topic is treated as covered by the explicit sudo/install-driver warning, which a small-project maintainer would accept as the intended reading of 'data-destructive' for a repo whose only persistent side effect is a system audio driver, not a data store.
- `G06`: The standard explicitly includes lockfiles among the paths that need marking; a tracked build-output directory being correctly marked does not cover a tracked lockfile that is not, so this is one part met (generated dirs) and one part missing (committed lockfiles) rather than a full Fail.
- `I01`: I01's own boundary states: 'A name with a version that differs from the release is Partial.' The embedded driver bundle is part of the one distributed artifact and its version (1.0.0) differs from the release (0.5.0), so the criterion's stated boundary applies directly.
- `I04`: The standard normally accepts reading source as evidence for a GUI app's About window, but since the release asset was fetchable it was read directly per the section's own rule that source-only values are not evidence for what the artifact carries; the mounted DMG is stronger, more current evidence than source and shows the links do not actually render in the shipped build.
- `I05`: 'One source image' is read as one consistent design asset reused across surfaces, which a small-project maintainer would accept even though the site copy is a separately checked-in, hand-resized export rather than an automated single build step producing both files.
- `I06`: 'Produced by the build' is read per the criterion's own text as either tag-derived or a single source-controlled value the build writes in; since one identity value (app short version) meets that bar cleanly while another (the embedded driver's version/copyright) is plainly hand-maintained and stale, this is the stated 'some is Partial' case.
- `L05`: Read the criterion's parts as dates, numbers, currency and sorting individually: dates and currency have nothing to assess (not applicable within this repo) and sorting is met, but the numbers part — the one part this repo actually exercises, on nearly every value in the mixer UI — is not met, giving "at least one part met, at least one missing" per the standard's multi-part default, i.e. Partial rather than Fail or Pass.
- `P11`: Read 'what it risks' as risk to the project's specific safety-critical paths (realtime audio callback, HAL plug-in) rather than a generic free-text risk field, since that is what a maintainer of this kind of project would actually want surfaced.
- `R01`: Per 'A criterion applies to ... its latest published release', R01 was assessed against the actual bytes of the current latest release (v0.5.0), not the unreleased source fix, matching the standard's own instruction that R04 is likewise read from the stamped/released value rather than the source placeholder.
- `R09`: Read GitHub's live secret-scanning and Dependabot alert state (both continuously-tracked, currently zero) as satisfying 'no open alert at the release', the same way B06 reads these APIs directly without requiring a separate release-time snapshot to be hand-recorded, since these are one of the three evidence forms the criterion names on their own, not the local-scan-command path that needs an extra recorded result.
- `S02`: Main entry point read as the audio/DSP processing chain and local-API control surface, since this is a GUI app whose logic is tested behind its views per the standard's own guidance.
- `S03`: Minimum reading of 'where supported' is a format-or-lint check plus a type check where the toolchain provides one; having only the type check without any format/lint check is explicitly the 'reverse' Partial case the standard names.
- `S04`: A claimed OS range ('13.0 or later') is read as covered by a job on the newest available runner, per the standard's own example for a stated range.
- `S07`: 'Actionable' read as naming the failed operation and its proximate cause, per the standard's stated minimum.
- `S10`: Read "non-obvious constraints" as constraints a new contributor could violate without being told, e.g. realtime-safety rules or the driver's intentional dumbness — the Architecture section names several of these explicitly, so the documentation goes beyond a bare component list.
- `S11`: Applied "no broader than the work requires" by checking each granted scope against what the job's steps actually do (checkout needs read; deploy-pages needs pages/id-token write; committing to a branch needs contents write) — none of the three workflows grants an unused scope such as write-all.
- `T01`: Applied the same blanket-N/A treatment the standard uses elsewhere for a whole criteria range when its gating profile does not apply (e.g. P01-P13 for private repositories when Public does not apply): the requirement does not reach the repository at all, per 'Not applicable where the repository has nothing the criterion is about... the requirement does not reach the repository at all' (Deciding Without The Maintainer, default results).
- `W03`: 'Who it is for' is read as satisfied by the clear use-case description (Teams/Zoom/OBS users mixing multiple mics) rather than requiring an explicit persona label — the reading a small-project maintainer would accept.
- `Y01`: Y01 covers 'collects, stores, or transmits'; the existing statement addresses collection/transmission but omits that the app stores settings and credentials locally, which is a real part of the source the statement doesn't mention -- an omission per the standard's own Partial boundary, not full Fail since a statement does exist and is otherwise accurate.
- `Y04`: Y04 is Partial where only some storage locations are documented; here the token/port files and one UserDefaults key are named but the settings and device-selection stores (the actual user data) are not, so a real, material part is missing.
- `Y06`: Not-applicable does not fit because persistent user data exists; some deletion behaviour (the port file) is stated but the durable stores (settings, token) have no retention/deletion statement, which is a real, material gap rather than a total absence -- Partial.
