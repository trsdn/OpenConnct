# Self-assessment against the trsdn Repository Quality Standard

- Assessed on: 2026-09-20
- Standard version: [1.15.0](https://github.com/trsdn/.github/blob/v1.15.0/docs/repository-quality-standard.md)
- Result: 51 pass, 18 partial, 14 fail, 21 not applicable, 0 unknown
- State: **Needs work**. There are recorded `Fail` results and none of them is on a critical criterion, which is the standard's rule for this state.

Every criterion was assessed by an AI agent that read the standard's text for it and the repository, and each `Fail` and `Partial` was then re-checked by a second agent that tried to refute it. The second pass changed no result. Nothing was left for the maintainer to decide. The assessor did not run the software itself where it says so below.

## Gaps

| Criterion | Result | Evidence |
|---|---|---|
| `B09` | partial | The standard (B09 note) has the homepage agree only when it is set and the README names it, or when it is empty and the README names no site. `gh repo view` shows homepageUrl https://trsdn.github.io/OpenConnct/, which is set. A grep of README.md for github.io, homepage, website and site finds no mention of it or of any site or docs address, so the homepage does not agree. Visibility PUBLIC agrees with the open-source README and licence, and isArchived false agrees with a README that announces no end of maintenance. Topics are excluded here (owned by B12/P07). One dimension agrees and one does not, which is the standard's Partial boundary, not Fail. No table reason or other means makes it Pass or na. Confirmed as the colleague assessed it. |
| `B13` | partial | Standard: Pass means no hand-maintained restatement of a command, version or policy. Partial means one that agrees with its home, and Fail means copies that disagree. Generated restatement does not count. Checked by grep: README.md restates make targets by hand (a table at lines 592-602, plus make test and make test-driver at 609 and 787). CONTRIBUTING.md lines 23-25 restate make test, make test-driver and make build. AGENTS.md lines 46-79 restate the Swift 6 toolchain and make test-all, and README lines 33 and 584 restate the Swift 6 toolchain again. None is generated and none links to a single home. I found nothing that disagrees, so this is Partial rather than Fail. No pass route applies: none of the copies is generated or a bare link, and nothing in the standard's table converts them to na or pass. |
| `B14` | partial | The colleague's result stands. In scope: the standard (section for B14) covers a repo that names a credential it expects to exist. Here .release.env.example names NOTARY_PROFILE and APPLE_APP_PASSWORD. README.md lines 697-705 list five "CI secrets" (MACOS_CERTIFICATE, MACOS_CERTIFICATE_PWD, APPLE_ID, APPLE_TEAM_ID, APPLE_APP_PASSWORD). README.md line 327 documents the local API token file api-token. Not met: grep for revok/rotat/regenerat/compromis in SECURITY.md, AGENTS.md, README.md, CONTRIBUTING.md, docs/ and integrations/ finds nothing about replacing an exposed credential or who does it. SECURITY.md covers only vulnerability reporting and design. AGENTS.md lines 98-100 and README lines 638-639 say only that credentials never enter the repo and the broker holds them, which names where they live but not revocation or replacement. No other-means or table reason applies, and .github/conformance.yml has B14 as unknown. The standard makes "names some of its credentials" a Partial, and not Not-applicable, since GITHUB_TOKEN is not the only credential named. Fail would need it to state nothing. |
| `B15` | fail | Update/Package.swift pins mxcl/AppUpdater exact 4.1.2 (Package.resolved also lists mxcl/Version). The Makefile builds it into the shipped app via `$(call update_obj,AppUpdater)` (line 47), which is the standard's "compiled artifact that statically links one" case. A repo-wide grep for AppUpdater, third-party, NOTICE, acknowledg and mxcl finds no notice file, attribution list or licence-obligation statement in README, AGENTS.md, CONTRIBUTING.md, docs/ or the app resources. README only mentions AppUpdater as the update mechanism (lines 303, 559, 586, 667), and LICENSE is the project's own MIT text. .github/conformance.yml has B15 as "unknown", so no earlier statement exists. The standard makes "redistributes and states nothing" a Fail, and Partial only applies where the approach is stated for some shipped code but not all. Neither the consumer-resolved exemption nor Not-applicable fits. I did not check GitHub-side settings. |
| `B16` | fail | Refutation failed; the colleague's fail stands. The standard (B16 section) says a ruleset or classic protection is equally acceptable, and a repository with neither that blocks force push and deletion gets Fail. Partial needs one of the two blocked, and the N/A row needs a private repository on a plan without rulesets. I read GitHub: `gh api repos/trsdn/OpenConnct/rulesets` returns `[]`, `.../branches/main/protection` returns 404 "Branch not protected", and `.../rules/branches/main` returns `[]`, so nothing covers main. The repository is public (visibility public, owned by a User), so both mechanisms are offered and the N/A and "platform lacks the capability" reasons do not apply. Automation Availability does not apply either, since B16 needs no runner. .github/conformance.yml has B16 as `unknown`, which is not an allowed result and records no reason. |
| `G08` | fail | Standard G08: an absent .github/github-app.yml is Fail where the platform offers the capability. Not applicable requires that the repo is not on GitHub or the account has no access to the app, with what was checked recorded. The repo is on GitHub, and .github/ holds only badges, conformance.yml, ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE.md and workflows. `gh api contents/.github/github-app.yml` returns 404. conformance.yml (G08: na) and docs/self-assessment.md say only "no app installed/configured". That is not the standard's no-access condition, and no access check is recorded. Partial does not fit, since that needs a committed but placeholder or contradicting file. Nothing found makes a better result correct, so I confirm Fail. |
| `I01` | partial | Standard I01 boundary: "A name with a version that differs from the release is Partial." The app Info.plist matches (name OpenConnct, version 0.5.0, stamped from the tag by scripts/build_release.sh via PlistBuddy). The embedded HAL driver bundle does not: source App/OpenConnctDriver/Info.plist hardcodes CFBundleShortVersionString 1.0.0 and CFBundleVersion 1, and no build script stamps it (scripts/lib_version.sh only syncs the app, the driver .pkg and the DMG filename), which matches the colleague's reading of the v0.5.0 zip. Release v0.5.0 exists, so the ships-nothing not-applicable case does not apply, and no table reason or other means makes it a pass. |
| `I02` | fail | I downloaded release v0.5.0 (OpenConnct-v0.5.0-macOS-universal.zip) and read OpenConnct.app/Contents/Info.plist. It has only name, id, version 0.5.0, build 66, icon, LSMinimumSystemVersion, high-res flag, principal class and the microphone usage text. It has no repository or issue-tracker key. Contents/Resources holds only AppIcon.icns and install-driver.sh, and install-driver.sh contains no URL. The app binary holds only the fragments "trsdn" and "https://api.github.com/repos/" (update check), not a repository URL or an issues URL. The standard says any key or bundled file whose name says what it holds is accepted, but nothing in the artifact carries either URL, so no boundary, table reason or other means gives partial or na. The artifact ships, so I02 applies. The GitHub URLs appear only in source and docs (README.md, SECURITY.md, .github/ISSUE_TEMPLATE, site/index.html), which the standard does not count. |
| `I03` | fail | I mounted the v0.5.0 release DMG (OpenConnct-v0.5.0-macOS-universal.dmg) and read the app. OpenConnct.app/Contents/Info.plist has no NSHumanReadableCopyright and no licence key. Contents/Resources holds only AppIcon.icns and install-driver.sh, so no LICENSE or acknowledgements file is bundled. The app binary has no MIT text or "Torsten" string, so an About resource cannot supply it. The LICENSE is MIT ("Copyright (c) 2025 Torsten Mahr"), whose term requires the notice in copies, so the bundled text is required. The only copyright field in the repo is App/OpenConnctDriver/Info.plist, which reads "OpenConnct" (not a holder name) and has no SPDX id. No other means, table reason or boundary gives a better result, and Partial does not fit because the app carries neither identifier nor holder. The colleague's fail is confirmed. |
| `I04` | partial | The standard accepts source that renders the version and both links for a GUI, and a site read from its fetched footer. I read the source and did not operate the product. site/index.html footer (lines 116-119) links the repository, the MIT licence, SECURITY.md and /issues (the issue tracker), but I found no version string on the page. A grep of App/ and integrations/opendeck for github.com/trsdn or an issues URL found nothing, and there is no custom About window, so the running app shows the plist version through macOS's standard About panel but no repository or issue links. The site and the app each cover only part of the criterion, so partial stands rather than fail or pass. I found no reason from the standard's table and no other means that would give a better result. |
| `I06` | partial | Confirmed partial. The standard says a value is build-produced if derived from the tag or set in one source-controlled place the build writes in, and that some build-produced values is Partial. scripts/build_release.sh runs PlistBuddy to stamp the app's CFBundleShortVersionString from resolve_version (scripts/lib_version.sh, git describe), and scripts/make_pkg.sh passes the same VERSION to pkgbuild. That covers the app version. The driver's App/OpenConnctDriver/Info.plist still hand-types CFBundleShortVersionString 1.0.0, CFBundleVersion 1 and NSHumanReadableCopyright "OpenConnct". The app's Info.plist hand-types CFBundleVersion 1 and its checked-in 1.0.0 is only a placeholder. No repository URL, issue URL or licence keys exist in either plist. I found no MARKETING_VERSION or other single-source mechanism, and no reason in the standard's table that would make this pass or not applicable. I did not check the live GitHub state. |
| `L01` | fail | The standard's L01 reading requires a one-sentence README statement of the primary language and says "Silence is a Fail" (repeated in German-Content Exception: "An undeclared language is a Fail on L01"). grep of README.md, CONTRIBUTING.md, site/index.html, docs/*.md finds no "primary language" or "English only" sentence. There is no locale manifest (no .strings, .xcstrings or .lproj found), and L01 has no partial boundary. The repo has user-facing surfaces (app, site, HTTP API), so it is not applicable. Only site/index.html line 2 has html lang="en", a technical attribute that is not a declaration. |
| `L03` | fail | Standard's L03 reading needs a README sentence ("English only" or a locale list) or a locale manifest (locales/, i18n config, string catalog). Grep across the repo (excluding build dirs) finds no "english only", "primary language" or "supported locales" text, and no .lproj, .xcstrings, .strings or locales/ files. The only hint is CFBundleDevelopmentRegion=en in App/OpenConnctApp/Info.plist, which is a bundle default and does not declare supported locales or English-only, so it is not a manifest. README has no language statement, and .github/conformance.yml still has L03 as unknown. Colleague's fail stands. |
| `L05` | partial | The standard's L05 text names toFixed( as a search hit for manual formatting and reserves 'Not applicable' for repos where nothing formats these values for display. Here there are 52 String(format:) calls in the Swift sources (Theme.swift "%+.1f dB" and "%.0f Hz", EffectPanels, MicDetailView, HeaderBar, NoiseFloorCalibration), plus toFixed(1) in the OpenDeck plugin logic.js. A grep of all Swift files finds no NumberFormatter, .formatted(, FormatStyle, Measurement or Locale, so displayed numbers are not locale-aware. I found no displayed dates, currency or user-read text sorting, so those parts have nothing to check. No reason in the standard's deviation table applies, and the standard has no exemption for technical units like dB and Hz. Some parts are met and one is not, so the default multi-part rule gives Partial. I found nothing to refute the colleague's result. |
| `L07` | partial | The colleague's partial holds. The standard (L07 reading) samples maintainer-opened issues among the last twenty, with no exception for history and none for the German-Content Exception, which covers the product surface only. I read the issues on GitHub. #8 (title and body), #9 (title German, body German, closed as superseded) and #10 (title and body German) are all maintainer-authored (trsdn) and fully German, and #9's title is actually German ('reviewen'), not English. The colleague's note that it was English is wrong, but that does not change the result. Issues 1-7, 12 and 13 are English, and the README, docs, commits and release notes are English per the colleague and the conformance file. None of the three German issues is excused: they are closed or not, but they fall in the sample, and no table reason or other means makes them pass. Most surfaces are English with a real gap in one surface, so partial rather than fail. |
| `P05` | partial | The standard says P05 is met per topic by one sentence or a link. Security and support status always apply, and some topic uncovered gives partial. The README covers install, configuration, examples, compatibility and security (link to SECURITY.md at line 80). It has no sentence or link on support status: no link to CONTRIBUTING.md and no mention of Issues. Status and roadmap (line 752) covers verified behaviour and limits only. SECURITY.md line 124 (Supported versions) and .github/ISSUE_TEMPLATE/config.yml contact links exist, but the README does not link them as support status. Nothing makes it not applicable, since this is an installable app, so the colleague's partial stands. |
| `P08` | fail | The README has no badge block. grep finds no image syntax; the only "badge" hits are the prose about the in-app MIC/APP badge at lines 465-467. .github/badges holds only conformance.svg, and the README does not reference it. The repository is public and not archived (gh repo view), so P08 applies. Nothing in the standard excuses this: the runner exists (ci.yml), the repo has a LICENSE and a macOS 13+ requirement in the README Requirements table, and releases are published (v0.5.0). So the required block (license, platform, CI, release, conformance) is all missing. There is no partial boundary for a block that is entirely absent. The "no release workflow" fact does not remove the release badge, because releases are still published. The colleague's result stands. |
| `P09` | fail | The standard's P09 text and its Repository Statistics Results paragraph both say a repository with a runner and no published card is a Fail. Runners are available: recent CI runs on main completed successfully, and ci.yml and pages.yml exist. No "no runner" state is recorded in .github/conformance.yml. There is no stats workflow, the only branch is main so there is no stats branch, and grep finds no stats or card reference in README.md or .github. No third-party card exists either, so there is no partial-credit boundary. The Not-applicable route needs no runner, which is not the case here. |
| `R01` | partial | The standard's R01 boundary reads: Partial when some properties have no home, and Fail only when none is set anywhere. It also says the repository must state which properties live where, and the metadata belongs in the artifact's Info.plist where the manifest format has no field. App/OpenConnctApp/Info.plist holds name (CFBundleName, CFBundleDisplayName), version (CFBundleShortVersionString 1.0.0) and a bundle id, but no description, licence (NSHumanReadableCopyright) or repository URL. No other plist in the repo holds them, and grep of README.md, AGENTS.md and docs/*.md finds no statement of where properties live. Core/Package.swift has only name, platforms and products. The colleague's evidence holds, so this is partial rather than fail because name and version have homes and the GitHub description, licence and homepage exist. It is not pass because three properties have no home and nothing states where each lives. |
| `R02` | fail | Standard: R02 needs a statement naming the versioning scheme and what consumers can rely on; Partial only if the scheme is named; Fail if nothing is stated. A case-insensitive search of the whole repository (excluding .git, node_modules, dist) for semver, semantic version, versioning, backward, breaking and "before 1.0" finds only README.md:686 (the VERSION stamping row, which names no scheme) and a code comment in Update/Sources/OpenConnctUpdate/UpdateManager.swift:8 mentioning "OpenConnct-<semver>.dmg". That comment is not a consumer-facing policy. CHANGELOG.md head names only the Keep a Changelog format, not a versioning scheme; entries numbered 0.4.0 and 0.5.0 do not state SemVer. No Partial boundary applies because no scheme is named, and nothing else in the repository documents the policy. GitHub release notes were not checked because the GitHub MCP failed to connect and no gh check was run. |
| `R05` | fail | The standard's fail row applies: no kit and no dated record exist, and a signed app can be checked automatically with codesign --verify and spctl --assess. I found no smoke kit and no record that names a version checked. In the repo, scripts/notarize_dmg.sh runs stapler validate and spctl --assess on the DMG, but only as a step inside the maintainer's local build. It takes a locally built DMG rather than the published file, is not documented as a kit, and leaves no record. scripts/build_release.sh and make_dmg.sh run codesign --verify the same way. The other codesign/spctl text is README troubleshooting for an installed driver, SECURITY.md design notes, and docs/PHASE1-VERIFICATION.md. The latest release, v0.5.0, ships DMG, zip, sha256 files, preflight-manifest.json and provenance.json. Those record the broker run and the hashes, and grep finds no verification or spctl result in them. ci.yml and pages.yml do not check a release artifact. .github/conformance.yml has R05 as unknown with no smoke-tests entry. Nothing in the standard's table gives a better result. An operator-free check is possible, so the no-operator pass row does not apply. Artifacts are published, so not-applicable does not apply either. |
| `R08` | partial | Colleague's partial holds. The standard treats a documented notarization broker as sufficient provenance, but requires the repository to state what a consumer can check and how (codesign and spctl), and what the record does not prove. README 'Releasing' (lines 635-660) documents the broker and lists provenance.json and *.sha256 by name. It gives no instruction for a consumer to verify the downloaded DMG. The only codesign/spctl text is README lines 719-722, a troubleshooting step on the installed driver, and SECURITY.md describes the app's own internal checks. Nothing states what the record does not prove. Conformance.yml has R08 as 'unknown', so there is no recorded statement either. A Fail is not right because the broker route is real and documented, and no false unavailability claim is made. |
| `S03` | partial | Compilation is the type check and runs in CI (.github/workflows/ci.yml runs swift build, swift test, make driver, make build UNIVERSAL=1); the standard counts compiling as the type check for a compiled language. The Makefile compiles the C driver with -Wall -Wextra -Werror (line 60), a partial lint-like check, but the C++ core has -Wall -Wextra without -Werror. No format, lint or static-analysis step exists (no .swiftlint.yml, .swiftformat, clang-format, clang-tidy, analyzer step, eslint or tsc; the naming-consistency job is a grep, not a code check). Some kinds run and others are absent, so Partial under S03's Every/some/none rule; not Fail because the type check runs, and not N/A because Swift and C have standard format and lint tools. |
| `S08` | fail | Not applicable does not hold: Update/Package.resolved pins third-party AppUpdater 4.1.2 and Version 2.2.1 from mxcl. There is no dependabot.yml or Renovate config; .github has only ci.yml and pages.yml. I found one nearby sentence, README.md ~line 667, which says bumping AppUpdater needs a reviewed broker change because the broker requires Update/Package.resolved to equal its reviewed copy. That describes a gate on bumps, not who updates dependencies or triages advisories, and it is not in SECURITY.md or CONTRIBUTING.md. SECURITY.md covers only reporting; AGENTS.md line 110 covers adding dependencies, not updating them. I did not re-check the colleague's API facts (alerts off, dependabot_security_updates disabled). The Partial boundary needs vulnerability alerts on, so it does not apply. Since no owner or process is named, the standard's rule gives Fail. The README bump-gate sentence is the closest thing to Partial, but I am keeping the colleague's result. |
| `S09` | fail | The standard says a ruleset or protection requiring an existing check is Pass, protection with no required check is Partial, and none on the default branch is a Fail. It is Not applicable only where S02 and S03 fail first, or where no runner is available. Neither holds here: .github/workflows/ci.yml exists, the last 5 CI runs on main succeeded, and Actions is enabled. The settings are readable, so the "unreadable gives Partial" rule does not apply. The API calls returned no ruleset (`gh api repos/trsdn/OpenConnct/rulesets` returned `[]`) and no active rule on main (`rules/branches/main` returned `[]`). The colleague's 404 on branches/main/protection was also confirmed. The owner is a User account (repo public), so no organization-level ruleset could apply. With no protection at all there is no Partial, and I found no other means, table reason or boundary that improves the result. |
| `W08` | partial | Refutation failed; partial confirmed. site/index.html has no contributor, architecture or changelog section, so it is not a Fail. It does restate facts whose homes are README.md or docs/ (200 ppm over two hours at README.md:771 and docs/PHASE1-VERIFICATION.md:284; 'New devices arrive muted' at README.md:246; the signed and notarized universal build at README.md:657). The site's only repository links are the repo root, /LICENSE, /SECURITY.md, /issues and /releases/latest, so none points to those homes. The standard's Partial boundary (repeated without a link to its home) fits. The site 'Source on GitHub' link to the repo root does not count as a link to the fact's home, because the drift test detail sits in docs/. The conformance.yml self-record says pass, but that predates 1.15.0 and does not change this. |
| `X01` | partial | The standard's X01 boundary for a native app says to read for gesture-only interactions and custom controls that replace standard ones. Partial applies where named controls are affected but the main flow is operable, and that fits here. App/OpenConnctApp/Views/ChannelStripView.swift has a custom fader built from DragGesture (line 91) and onTapGesture(count: 2) (line 87), with only an accessibilityAdjustableAction (line 109) as an equivalent. There is no focusable, onKeyPress or onMoveCommand handling anywhere in the Swift source, and line 434 is a second onTapGesture(perform: onSelect). The main flow stays operable by keyboard through the standard Slider in MicDetailView, and no other criterion or table reason gives a better result. The site has visible focus outlines. The assessment is source-only, with no run or documented manual check, so it cannot reach Pass. |
| `X03` | partial | Confirmed the colleague's partial; no evidence found to change it. The standard says Partial where at least one property holds and a named gap remains in another. Contrast and colour hold, so it cannot be Fail. Sizing is a real gap: App/ has 28 Font.system(size:) uses and Theme.swift sets fixed sizes (labelFont 11, valueFont 11, captionFont 10, titleFont 12). A grep for platform text styles (.font(.body/.caption/.headline...)) or @ScaledMetric in App/ returned 0 hits. No table reason or other means (such as a documented check) applies, since conformance.yml still has X03 as unknown. The forced dark theme (SettingsView.swift:23 .preferredColorScheme(.dark)) is a further appearance gap but is not required to reach Partial. |
| `X05` | fail | The standard's X05 text: Pass = limitations stated or "none known" stated; Partial = a statement exists but omits a limitation found under X01-X03; Fail = no statement at all. I searched README.md, AGENTS.md, CHANGELOG.md, CONTRIBUTING.md, docs/, site/, integrations/ and .github/ for accessib/voiceover/screen reader. The only hit is docs/self-assessment.md, which lists good practice on the site and says X01-X05 are "still unassessed"; that is an internal assessment note and does not state limitations to a reader. README "Known limitations" (line 789) lists only physical inputs and stereo summing. There is no ACCESSIBILITY file and .github/conformance.yml has X05 as unknown. No statement exists, so the Partial boundary (a statement that omits a gap) is not reached. Neither a table reason nor N/A applies, since the app and the Pages site both have a UI. The colleague's Fail is correct. |
| `Y01` | partial | The standard says Y01 has no na result, and it says Partial where the statement omits something the source shows. It says Fail only where there is no statement or the statement contradicts source. site/index.html (Privacy) and README.md "Checking for updates" state "collects nothing" and name the daily GitHub check. Source does not contradict them. Neither text says what is stored locally. Stored data is only mentioned in passing: README.md:143 says settings are persisted per device UID, README.md:315 names the UserDefaults key updates.automaticChecks.v1, and README.md:327 and integrations/opendeck/README.md:90 name the api-token and api-port files. No single statement lists the stored data, so this is not met as written. It is Partial, not Fail, because a statement exists and the scattered mentions cover much of the stored data. I found no reason in the standard's table that would give a better result. The colleague's Partial stands. |
| `Y04` | partial | The standard's Y04 boundary says Pass when the storage location is documented and reachable, Partial where only some locations are documented, and Fail where none is. I found nothing that lifts it to Pass. README.md:327 and :338 name ~/Library/Application Support/OpenConnct/ only as the home of api-token and api-port. README.md:315 documents the UserDefaults key updates.automaticChecks.v1. The other stores are not documented: SettingsStore.swift and DeviceSelectionStore.swift write into AppSupport.directory, and AudioEngine.swift and LocalAPIController.swift write UserDefaults keys for the latency profile and API-enabled. docs/PHASE1-VERIFICATION.md:457 calls the directory a settings file safe to delete, but that is a verification doc and not user-facing. No README section covers user data, and no other Markdown or HTML file documents the remaining stores. It is not Fail because some locations are documented, and not na because the app writes user data. |
| `Y06` | partial | The standard's Y06 needs retention and deletion behaviour stated in README or runbook, or an explicit not-applicable result. It has no boundary text of its own. Not-applicable does not fit, because the app writes persistent data to ~/Library/Application Support/OpenConnct/ (settings, api-token, api-port). README.md lines 327-334 state only that api-port is removed when the API is switched off. They do not say the api-token or settings persist, or how to delete them. docs/PHASE1-VERIFICATION.md line 457 says the directory is safe to delete, but it sits in a verification doc and does not state retention. site/index.html's Privacy section covers collection (Y01), not retention. .github/conformance.yml lists Y06 as unknown, so there is no recorded not-applicable result. Some deletion behaviour is stated, so this is partial rather than fail. I found nothing that makes a better result correct. |

## Everything else

| Criterion | Result | Evidence |
|---|---|---|
| `A01` | not applicable | gh repo view trsdn/OpenConnct --json isArchived returned isArchived:false, so the repository is not archived and the Archived profile does not apply. |
| `A02` | not applicable | Not applicable because gh repo view reports isArchived:false; the README was checked and describes an actively maintained project, so the Archived profile does not apply. |
| `A03` | not applicable | Not applicable because the repository is not archived (isArchived:false per gh repo view); README and description name no successor and none is expected. |
| `A04` | not applicable | Not applicable because the repository is not archived (isArchived:false per gh repo view); it has a live Pages site and CI workflows, which is normal for an active repository. |
| `B01` | pass | GitHub description: 'Run several USB microphones as one system-wide input on macOS: per-channel DSP, drift-corrected mixing, and its own CoreAudio virtual device.' It says what the project does well beyond the name; homepage is set to the Pages site. |
| `B02` | pass | README.md opens with purpose and a 'Why it exists' paragraph. The Requirements table (macOS 13+) and the 'What it is not' scope give the audience. It has Install, Using it, and Building and testing sections. A 'Status and roadmap' section gives the status. Key links include the latest release, docs/*.md, and the [MIT](LICENSE) link. |
| `B03` | pass | LICENSE holds the full MIT text (Copyright 2025 Torsten Mahr). GitHub licenseInfo reports key 'mit', and README.md ends with a licence line linking to it. |
| `B04` | pass | git ls-files shows no .env, key, certificate, dmg, or build output. Only .release.env.example (a placeholder) is tracked. .gitignore covers dist/, build/, .build/, *.app, *.driver, *.dmg, *.pkg, .release.env, .DS_Store, _site/, and the bench and probe build dirs. .worktrees is excluded via .git/info/exclude and not tracked. The untracked .remember directory was not checked against .gitignore and is not in the tracked list. |
| `B05` | pass | README (Building and testing) and AGENTS.md document `make test`, `make test-all`, and `npm test` in integrations/opendeck; the toolchain (Xcode CLT, Swift 6) is stated in the README Requirements table. The latest CI workflow runs on main are green: 35522400903 (2026-09-20) and the two before it, all success. I did not run the command myself. |
| `B06` | pass | Merge policy: no ruleset and no branch protection (404), and squash, merge commit and rebase are all enabled. No merge method is stated in README, CONTRIBUTING or AGENTS.md. The standard's single-maintainer exception applies (git shortlog and the contributors API show one contributor), so the merge policy is met. Alerts: Dependabot alerts are disabled (no alerts). Code scanning has no analysis (404). The secret-scanning list returned 0 open alerts in the first, successful call. A repeat call, which failed on token scope, is not evidence. |
| `B07` | pass | Update/Package.swift declares the one third-party dependency, AppUpdater pinned exact 4.1.2, with Update/Package.resolved tracked as the lockfile. Runtimes are declared: macOS 13 in the README and Update/Package.swift, macOS 14 for tests in Core/Package.swift, and Swift 6 in the README. The OpenDeck plugin has no dependencies (package.json has none) and states Node 20 as the floor in ci.yml, with manifest.json carrying a Nodejs entry. |
| `B08` | pass | The latest release is v0.5.0 (2026-09-20). CHANGELOG.md has a '[0.5.0] - 2026-09-20' entry (Added, Fixed), and 0.4.0 and earlier entries follow Keep a Changelog. The [Unreleased] section is empty, and the last main commit promoted the changelog entry to 0.5.0. |
| `B10` | pass | The repo is owned by the personal account trsdn, which per the standard is the owner with no CODEOWNERS needed. The latest commit on the default branch is 2026-09-20, within the six-month cadence, which meets the maintenance-status part. |
| `B11` | pass | .github/conformance.yml exists with standard_version 1.12.0 and assessed_on 2026-09-19, well within the six-month cadence. The record is against an older standard version, but B11 asks only for a record with a date inside the cadence. |
| `B12` | pass | `gh repo view --json repositoryTopics` lists `trsdn-standard` among the topics. |
| `D01` | not applicable | Not a standing deployment. The row matched is an application downloaded from a release and installed (a macOS app), so the Package profile decides it. I looked for a service, container, launch agent, daemon or scheduled job installed by the repo and found none. The local HTTP API runs only while the user's app runs and is off by default. GitHub Pages is a static host and belongs to the Published Site profile. |
| `D02` | not applicable | Not a standing deployment. The row matched is a macOS app installed from a release. The Package profile decides it, so D02 is not assessed. |
| `D03` | not applicable | Not a standing deployment. The row matched is a macOS app installed from a release. The Package profile decides it, so D03 is not assessed. |
| `D04` | not applicable | Not a standing deployment. The row matched is a macOS app installed from a release. The Package profile decides it, and the Pages site is a static host with no server the maintainer runs. |
| `D05` | not applicable | Not a standing deployment. The row matched is a macOS app installed from a release. The Package profile decides it, so D05 is not assessed. |
| `D06` | not applicable | Not a standing deployment. The row matched is a macOS app installed from a release. The Package profile decides it, and the repository runs no host holding state. |
| `G01` | pass | AGENTS.md exists at the repository root (tool-neutral name). |
| `G02` | pass | AGENTS.md has purpose (What this repository is), a Layout table, Setup, Run (make run), and names `make test-all` as the single validation command; Makefile:171 defines test-all as test + test-driver. Not run locally (read-only); the linked green CI runs on main (2026-09-20, success) execute swift test and make test-driver, which counts as a green run per B05. |
| `G03` | pass | AGENTS.md 'Do not these' section: no history rewriting or force push, no secrets/credentials, no deploy/release/tag/settings changes, no make install-driver/uninstall-driver or sudo scripts (data/system-destructive), no hand-editing generated dirs. All six topics covered; none treated as absent. |
| `G04` | pass | None of .github/copilot-instructions.md, CLAUDE.md, GEMINI.md, .cursorrules, .cursor/, .windsurfrules, .clinerules exists, so there is nothing to diverge (standard says Pass, not na). |
| `G05` | pass | AGENTS.md 'Validate before proposing a change' names `make test-all` as the single command that must succeed; it is in the Makefile and CI runs its parts (swift test, make test-driver) green on main, latest run 2026-09-20 success. |
| `G06` | pass | .gitignore excludes dist/, build/, .build/, _site/, tools/*/build/; AGENTS.md 'Generated, never hand-edit' lists dist/, _site/, Core/.build/, Update/.build/, tools/*/build/. No .gitattributes, not needed since AGENTS.md and .gitignore mark the paths. |
| `G07` | pass | AGENTS.md 'Attribution' section documents that agent changes carry Co-Authored-By/session trailers and are reviewed like any other change before merging to main (a documented trailer rule and review expectation). |
| `I05` | pass | The released app contains Contents/Resources/AppIcon.icns with CFBundleIconFile AppIcon, generated by scripts/make-icon.py. site/assets/icon.png (256px, used as favicon and header in site/index.html) is the same image as App/OpenConnctApp/Resources/AppIcon.png resampled (mean pixel difference about 1.1 of 255), so one source image serves both surfaces. |
| `L02` | pass | Sampled Swift UI/view files, Core control messages (e.g. NoiseFloorCalibration.swift), the OpenDeck plugin, site/index.html and scripts; a grep for German function words in source, HTML, JS, C and shell finds none (only 'MIT' license hits). All user-facing strings found are English. |
| `L04` | not applicable | The repository ships no string catalogs (no .xcstrings/.strings/.lproj found) and only one locale, so it has no localized builds. |
| `L06` | not applicable | The repository ships no translations (no catalogs or locale directories found), so there is nothing to trace. |
| `P01` | pass | Root LICENSE holds the unmodified MIT text; the GitHub API reports spdx_id MIT (community/profile and repo view), and MIT is on the OSI list. |
| `P02` | pass | CONTRIBUTING.md and CODE_OF_CONDUCT.md (Contributor Covenant) are both at the repository root; the community profile lists both. |
| `P03` | pass | SECURITY.md at the root tells reporters to use GitHub Security Advisories, not a public issue, and gives response times. GET /private-vulnerability-reporting returns {enabled:true}. |
| `P04` | pass | .github/ISSUE_TEMPLATE has bug_report.yml and feature_request.yml (forms), plus config.yml with blank issues disabled. .github/PULL_REQUEST_TEMPLATE.md exists. |
| `P06` | pass | GET community/profile returns health_percentage 100 with readme, license (MIT), contributing and code_of_conduct all recognised. The null issue_template is not counted here; P04 covers it. |
| `P07` | pass | The repository has a non-empty description and 13 topics including audio, macos and coreaudio. The homepage https://trsdn.github.io/OpenConnct/ returned HTTP 200 (Pages site exists, so a homepage is required). |
| `P10` | pass | bug_report.yml has required fields for what happened and what was expected, steps to reproduce, macOS version and architecture, OpenConnct version, and hardware. It also has an optional diagnostics field. |
| `P11` | pass | .github/PULL_REQUEST_TEMPLATE.md has Summary (what and why, link issue as Closes #N), Type of change, Validation (make test, test-driver, build, hardware), Realtime safety and Plug-in safety (risk), Measurements and Documentation sections. |
| `R03` | pass | Annotated tag v0.5.0 exists on origin and peels to commit ef5281af. The release's provenance.json records source.repository trsdn/OpenConnct, source.tag v0.5.0 and source.commit_sha ef5281af, which is that same commit, plus the broker run id and commit. README 'Releasing' documents the tag command and 'scripts/request.sh openconnct vX.Y.Z --publish' from the broker checkout. The uploaded DMG, ZIP and checksums are on the release, and provenance.json lists their sha256. |
| `R04` | pass | Latest release: tag v0.5.0 and title 'v0.5.0'. The broker's provenance.json and preflight-manifest.json both record version 0.5.0. README lines 651 and 686 document that the tag's version is stamped into the app and driver. The Info.plist in the source tree still holds 1.0.0. |
| `R06` | pass | The v0.5.0 release body is the changelog entry. It names user-visible changes: the new Delay setting with measured latencies, and the fix for the random 340 ms extra delay. It warns that the driver must be updated and that installing it restarts system audio for about a second. Nothing in the release is breaking or raises a minimum requirement. |
| `R07` | pass | The v0.5.0 release body matches the CHANGELOG.md [0.5.0] entry verbatim, and the entry is non-empty. CHANGELOG has an empty [Unreleased] section. README 'Releasing' documents that the broker publishes the CHANGELOG entry as the notes and refuses to publish a version with no entry or with anything left under Unreleased. |
| `S01` | pass | Update/Package.resolved is committed and pins the one third-party SwiftPM dependency (AppUpdater); Core/Package.swift and integrations/opendeck/package.json have no third-party dependencies. README 'Building and testing' and CONTRIBUTING.md document make build/test/test-driver and npm test. |
| `S02` | pass | Core/Tests holds OpenConnctControlTests (local API auth, dispatcher, routing, request parsing) and OpenConnctDSPTests, plus integrations/opendeck/test; failure paths are asserted (LocalAPIDispatcherTests testUnauthenticatedRequestGets401, LocalAPIRequestTests .invalid framing and nil parse). ci.yml runs swift test, make test-driver and npm test, and recent CI runs on main succeeded (gh run list). |
| `S04` | pass | README claims macOS 13+ (Package.swift .macOS(.v14)), arm64 and x86_64 universal; ci.yml runs on macos-latest and builds with UNIVERSAL=1, and runs the Node plugin on Node 20 (the stated minimum). |
| `S05` | pass | gh api repos/trsdn/OpenConnct security_and_analysis: secret_scanning enabled and secret_scanning_push_protection enabled. |
| `S06` | pass | Configuration comes from the environment (OPENCONNCT_METER_HZ, OPENCONNCT_SOAK_LOG in ParameterStore.swift; .release.env is gitignored with a committed .release.env.example). No home-directory paths or credentials found by grep; the example carries the maintainer's Developer ID identity name and team ID, which are public in every signed release, and no password or token. |
| `S07` | pass | Logging is NSLog in LocalAPIServer.swift, AudioEngine.swift and ParameterStore.swift; messages name the operation and cause (e.g. 'local API could not create a listener: <error>', 'could not set buffer size %u on device %u (status %d)'). The API token and request headers/bodies are not logged; README documents the render path as log-free. |
| `S10` | pass | README 'Architecture' and 'Repository layout' name the components (driver, DSP core, app, Update, integrations) and constraints (realtime-safety rules, Developer ID signing requirement, mirror-device design, broker notarization); AGENTS.md documents generated files not to hand-edit, the OpenConnect legacy-name allowlist enforced by CI, and no-new-SwiftPM-dependencies. |
| `S11` | pass | ci.yml declares workflow-level permissions: contents: read; pages.yml declares contents: read and a deploy job with pages: write and id-token: write, which actions/deploy-pages requires. |
| `S12` | pass | All external references in ci.yml and pages.yml are GitHub-owned actions (actions/checkout@v4, setup-node@v4, configure-pages@v5, upload-pages-artifact@v3, deploy-pages@v4) pinned by major-version tag, which the standard allows; no third-party actions or reusable workflows. |
| `S13` | not applicable | Neither ci.yml (push, pull_request) nor pages.yml (push, schedule, workflow_dispatch) uses pull_request_target or workflow_run, and no workflow reads a secret. |
| `T01` | not applicable | Documentation profile does not apply: the repository's primary product is a macOS app (Swift app, C driver, C++ DSP core) released as a DMG, with docs (README, docs/) secondary to it. |
| `T02` | not applicable | Documentation profile does not apply: primary product is the macOS app, not documentation; I checked README.md, docs/ and the release assets. |
| `T03` | not applicable | Documentation profile does not apply: primary product is the macOS app, not documentation. |
| `T04` | not applicable | Documentation profile does not apply: primary product is the macOS app, not documentation. |
| `T05` | not applicable | Documentation profile does not apply: primary product is the macOS app, not documentation. |
| `W01` | pass | .github/workflows/pages.yml builds site/ with scripts/build_site.sh and deploys via configure-pages, upload-pages-artifact and deploy-pages on push to site/**, weekly and on dispatch. AGENTS.md line 35 documents that site/ is the Pages landing page, that build_site.sh fills the date and that pages.yml deploys it. |
| `W02` | pass | gh repo view gives homepageUrl https://trsdn.github.io/OpenConnct/. site/index.html is a single page with a repository link in the header ('Source on GitHub') and in the footer nav ('Source code'). |
| `W03` | pass | In site/index.html the hero comes right after the header. It gives the name and what it is ('a Mac app that mixes several USB microphones into one virtual input'), who it is for (Teams, Zoom and OBS users) and the status line 'Maintained. Needs macOS 13 or later.' Only the header navigation precedes it. Source read only, page not rendered. |
| `W04` | pass | Landing source has all seven items. It gives the name and a one-sentence purpose. Status is 'Maintained', and the download button targets releases/latest, which counts as naming the release under the standard's 'links to it' wording. It has the feature list plus a mixer.png screenshot, a Get it section with a download button, and a Privacy section saying nothing is collected. The footer links to source, license, SECURITY.md and issues, and the page states 'Page last reviewed {{DATE}}', filled by build_site.sh. |
| `W05` | not applicable | Retired 2026-09-17 (standard 1.12.0); the catalog row says it is not assessed, and the record carries it as na with the rationale 'retired'. |
| `W06` | not applicable | Retired 2026-09-17 (standard 1.12.0); the catalog row says it is not assessed, and the record carries it as na with the rationale 'retired'. |
| `W07` | pass | Source review only, no browser run. site/index.html has no <script>. Its only src/href values are relative (style.css, assets/icon.png, assets/mixer.png) or github.com links a visitor follows. site/style.css has no @import or url() and uses system font stacks. There are no cookie writes and no analytics. Hosts requested: none other than the site itself. |
| `W09` | pass | site/style.css (271 lines) is a hand-written stylesheet with its own palette tokens (--ink, --button #d0342c, --trace-drift and others) for dark and light, a rounded system type stack, and a bespoke SVG figure of drifting versus locked waves. There is no framework or template theme and no other project's stylesheet. Source only, page not rendered. |
| `X02` | pass | Of 48 accessibility modifier uses in App/OpenConnctApp/Views, the icon-only buttons all carry labels: Mute, Solo, Open settings, Remove microphone, Add microphone, effect toggles, header buttons, HPF modes. The custom fader has a label 'Fader', a value and an adjustable action, and Theme.swift sliders label each Slider. The site has landmarks, alt text on the screenshot and role=img with aria-label on the figure. Source review only. |
| `X04` | not applicable | The shipped product is a GUI app, a local HTTP API returning JSON, and a site; none is terminal output. tools/*.swift and scripts/*.sh only print plain text and no colour or escape-sequence use was found (grep for NO_COLOR, isatty, ESC codes returned nothing). Those are developer probes, not a product surface. |
| `Y02` | pass | README.md 'Checking for updates' names the one outbound destination (GitHub releases via AppUpdater, api.github.com, DMG download), its purpose and daily frequency, and states it is the only outbound request the app makes on its own. Source grep of App/, Update/ and integrations/ found no other URLSession or URL use; LocalAPIServer is a 127.0.0.1 listener (documented in README), not an outbound destination. |
| `Y03` | pass | Source review of App/, Update/Package.swift and integrations/ found no Sentry, Crashlytics, Firebase, analytics or telemetry code or dependency (the only dependency is AppUpdater). README and site also state no analytics or telemetry. |
| `Y05` | pass | No source sends user content to any third party or AI provider; the only outbound request is the GitHub update check. README ('only outbound network request') and site/index.html Privacy together state that nothing else is contacted. |

## Readings applied

Where a criterion uses a judgement word, the reading used is stated here so it can be checked.

- `B04`: Ecosystem output for SwiftPM, clang and Node is read as covered by .build/, build/, dist/ and the Node plugin having no dependencies to install.
- `B05`: The documented command is `make test-all`; its green CI run on the default branch is accepted as the run, with the assessor not running it locally.
- `B06`: The single-maintainer default is read as met because there is one contributor and no ruleset, per the standard. A stated merge method would be a small improvement.
- `B07`: The Node floor is stated in CI and the plugin manifest rather than in package.json engines; that is read as declared for a dependency-free plugin.
- `B09`: Homepage agreement follows the standard: set and named by README, or empty and README names no site.
- `B10`: Maintenance-status part met via the standard's rule that a commit to the default branch within the review cadence counts.
- `B13`: A make-target list repeated in README, CONTRIBUTING and AGENTS counts as hand-maintained restatement of a command; none links to a single home.
- `B14`: Credentials are in scope because the repo names them. It names where the Apple ones live (the broker) but not revocation or replacement, so Partial.
- `G02`: Green CI run of the same steps (swift test, make test-driver) accepted in place of running make test-all in a read-only assessment.
- `G08`: Absent file where the platform offers it is Fail per G08 text; no evidence of missing account access was found.
- `I01`: The embedded driver is part of the shipped artifact and the repo states no separate driver versioning, so its differing version makes the name-and-version pair partial.
- `I04`: Graphical app cannot be operated here; I read the source and did not run the product. Version is shown, links are not, so one part is met and one is missing.
- `I05`: Only surfaces the repo has are assessed: app and Pages site; no store listing exists.
- `L02`: Sample of the files holding user-facing strings plus a German-word grep; no non-English text found.
- `L05`: Fixed-format technical readouts with unit suffixes (dB, Hz, ms) are still display formatting and not locale-aware, but are the only gap, so Partial rather than Fail.
- `L07`: Every surface sampled but issues is English; at least two of twelve issue titles (#8, #10) are German, so one part is missing: Partial.
- `P05`: Support status is read as a statement of maintenance level or where to ask for help; a known-limitations list does not say that.
- `P10`: Expected and actual result count as covered by one required field titled 'What happened' whose description asks for both.
- `R01`: The app is the released package. Its Info.plist is its home for name and version. Description, licence and repository URL have no home in the artifact and no statement of where they live instead, so some properties have no home.
- `R03`: This is the standard's second form, a shared release pipeline started for a specific tag. Its documented start command is in README.
- `R04`: R01 places the version in the artifact's Info.plist, which the broker stamps from the tag. I read the stamped value through the broker's recorded version 0.5.0 and did not inspect the DMG's plist. The 1.0.0 in source is treated as a placeholder, as the README documents.
- `R05`: The published app is signed and notarized, so a signature and launch-policy check needs no operator. The artifact can be checked automatically, so the 'no kit and no record' row applies.
- `R06`: A specific user-visible change is a named feature or fix with its effect. A warning is needed only for breaking changes or a raised minimum, and this release has none.
- `R07`: The gate is in the shared broker pipeline, and the repository documents it as the standard permits. I accepted that documentation without testing the pipeline.
- `R08`: The broker route qualifies as the shared pipeline's verifiable record. The criterion also asks the repository to state what a consumer can check and how, and that statement is missing.
- `S02`: For a graphical app the entry point is read as the logic behind its actions, reached without views: the DSP core and the control/local-API logic are tested that way.
- `S03`: Minimum reading: a format or lint check that runs automatically plus a type check; the type check exists, the format/lint check does not.
- `S04`: A range claim ('macOS 13 or later') is covered by a job on the newest runner version, per the standard; universal build covers both architectures at compile time.
- `S06`: Read 'personal data as a default' as private data; a public signing identity name and team ID in an example is not private.
- `S07`: Actionable = names the failed operation and its cause.
- `W03`: The audience is stated through the named use (Teams, Zoom, OBS callers), not as a 'for X' sentence; a small-project maintainer would accept that.
- `W04`: 'Which release the page describes' is met by linking to the latest release, as the 1.15.0 text allows; the earlier partial was against the 1.12.0 wording.
- `W08`: A one-line marketing restatement that does not link to the fact's home is counted as repetition, per the standard's Partial wording; a link from each fact to its README section would make it Pass.
- `X01`: Partial because one named control has no keyboard path outside VoiceOver while the main flow works; the native-app assessment is limited to source.
- `X03`: Sizing gap is fixed point sizes for body-level labels in the native app, with no platform text styles.
- `Y01`: 'Stores' is read as including local persistence of settings files, not only collection or transmission.
- `Y04`: 'User data' is read as the settings the app persists for the user.
- `Y06`: A maintainer-facing verification note is read as not equal to a stated user-facing deletion behaviour.
