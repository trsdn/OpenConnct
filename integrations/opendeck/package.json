{
  "name": "openconnct-opendeck",
  "version": "0.1.0",
  "private": true,
  "description": "A plugin for OpenDeck and the Elgato Stream Deck, so hardware keys can mute OpenConnct microphones and set their gain.",
  "type": "module",
  "scripts": {
    "test": "node test/manifest.js && node test/deck-socket.js && node test/logic.js && node test/smoke.js",
    "install-plugin": "./install.sh"
  },
  "license": "MIT"
}
